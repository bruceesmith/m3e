{{ $module := .Name }}{{ $count := len .Attributes }}
// --- Setters ---
{{ range $index, $val := .Attributes }}
{{- /* Add a blank line between functions */}}
{{- if $index }}{{ "\n" }}{{ end }}
/// {{ $val.Name }} sets the value of {{ $val.Name }} for this {{ $module }}.
///
{{ getSignature $count $val.Name $module $val.Type }} {
 {{- if gt $count 1 }}
 {{ $module }}(..record, {{ $val.Name }}: {{ $val.Name }})
 {{- else }}
 {{ $module }}({{ $val.Name }}: {{ $val.Name }})
 {{- end }}
}
{{- end }}
