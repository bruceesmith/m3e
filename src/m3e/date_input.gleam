//// DateInput is a segmented input for entering date and/or time values using a keyboard.
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/list
import gleam/option.{type Option, None}
import lustre/attribute.{type Attribute}
import lustre/element.{type Element}
import m3e/attr
import m3e/date.{type Date}
import m3e/date_input_time_format.{type DateInputTimeFormat}
import m3e/date_input_type.{type DateInputType}
import m3e/time_parts.{type TimeParts}

// --- Types ---

/// DateInput is a View Model for this component
///
/// ## Fields:
///
/// - value: The value of the input.
/// - type_: The interaction mode for editing date and/or time values.
/// - show_seconds: Whether to show seconds.
/// - time_format: Format used when editing time values.
/// - min_date: The minimum date that can be selected.
/// - max_date: The maximum date that can be selected.
/// - min_time: The minimum time that can be selected.
/// - max_time: The maximum time that can be selected.
/// - month_label: The accessible label given to the month segment.
/// - day_label: The accessible label given to the day segment.
/// - year_label: The accessible label given to the year segment.
/// - hour_label: The accessible label given to the hour segment.
/// - minute_label: The accessible label given to the minute segment.
/// - second_label: The accessible label given to the second segment.
/// - period_label: The accessible label given to the period segment (AM/PM).
/// - disabled: Whether the element is disabled.
/// - readonly: A value indicating whether the element is read-only.
/// - required: Whether a value is required for the element.
/// - name: The name that identifies the element when submitting the associated form.
///
pub opaque type DateInput {
  DateInput(
    value: Option(Date),
    type_: DateInputType,
    show_seconds: ShowSeconds,
    time_format: DateInputTimeFormat,
    min_date: Option(Date),
    max_date: Option(Date),
    min_time: Option(TimeParts),
    max_time: Option(TimeParts),
    month_label: String,
    day_label: String,
    year_label: String,
    hour_label: String,
    minute_label: String,
    second_label: String,
    period_label: String,
    disabled: Disabled,
    readonly: Readonly,
    required: Required,
    name: String,
  )
}

/// ShowSeconds is whether to show seconds.
///
pub type ShowSeconds {
  IsShowSeconds
  IsNotShowSeconds
}

/// Disabled is whether the element is disabled.
///
pub type Disabled {
  IsDisabled
  IsNotDisabled
}

/// Readonly is a value indicating whether the element is read-only.
///
pub type Readonly {
  IsReadonly
  IsNotReadonly
}

/// Required is whether a value is required for the element.
///
pub type Required {
  IsRequired
  IsNotRequired
}

// --- Defaults ---

pub const default_value: Option(Date) = None

pub const default_type_: DateInputType = date_input_type.Date

pub const default_show_seconds: ShowSeconds = IsNotShowSeconds

pub const default_time_format: DateInputTimeFormat = date_input_time_format.OneTwo

pub const default_min_date: Option(Date) = None

pub const default_max_date: Option(Date) = None

pub const default_min_time: Option(TimeParts) = None

pub const default_max_time: Option(TimeParts) = None

pub const default_month_label: String = "Month"

pub const default_day_label: String = "Day"

pub const default_year_label: String = "Year"

pub const default_hour_label: String = "Hour"

pub const default_minute_label: String = "Minute"

pub const default_second_label: String = "Second"

pub const default_period_label: String = "Period"

pub const default_disabled: Disabled = IsNotDisabled

pub const default_readonly: Readonly = IsNotReadonly

pub const default_required: Required = IsNotRequired

pub const default_name: String = ""

// --- Configuration ---

/// Config is a public record for configuring this component.
///
pub type Config {
  Config(
    value: Option(Date),
    type_: DateInputType,
    show_seconds: ShowSeconds,
    time_format: DateInputTimeFormat,
    min_date: Option(Date),
    max_date: Option(Date),
    min_time: Option(TimeParts),
    max_time: Option(TimeParts),
    month_label: String,
    day_label: String,
    year_label: String,
    hour_label: String,
    minute_label: String,
    second_label: String,
    period_label: String,
    disabled: Disabled,
    readonly: Readonly,
    required: Required,
    name: String,
  )
}

/// default_config is the default configuration for this component.
///
pub fn default_config() -> Config {
  Config(
    value: None,
    type_: date_input_type.Date,
    show_seconds: IsNotShowSeconds,
    time_format: date_input_time_format.OneTwo,
    min_date: None,
    max_date: None,
    min_time: None,
    max_time: None,
    month_label: "Month",
    day_label: "Day",
    year_label: "Year",
    hour_label: "Hour",
    minute_label: "Minute",
    second_label: "Second",
    period_label: "Period",
    disabled: IsNotDisabled,
    readonly: IsNotReadonly,
    required: IsNotRequired,
    name: "",
  )
}

// --- Constructors ---

/// from_config creates a new DateInput from the given configuration.
///
pub fn from_config(config: Config) -> DateInput {
  DateInput(
    value: config.value,
    type_: config.type_,
    show_seconds: config.show_seconds,
    time_format: config.time_format,
    min_date: config.min_date,
    max_date: config.max_date,
    min_time: config.min_time,
    max_time: config.max_time,
    month_label: config.month_label,
    day_label: config.day_label,
    year_label: config.year_label,
    hour_label: config.hour_label,
    minute_label: config.minute_label,
    second_label: config.second_label,
    period_label: config.period_label,
    disabled: config.disabled,
    readonly: config.readonly,
    required: config.required,
    name: config.name,
  )
}

/// new creates a new DateInput with the default configuration.
///
pub fn new() -> DateInput {
  from_config(default_config())
}

// --- Setters ---

/// value sets the value of value for this DateInput.
///
pub fn value(record: DateInput, value: Option(Date)) -> DateInput {
  DateInput(..record, value: value)
}

/// type_ sets the value of type_ for this DateInput.
///
pub fn type_(record: DateInput, type_: DateInputType) -> DateInput {
  DateInput(..record, type_: type_)
}

/// show_seconds sets the value of show_seconds for this DateInput.
///
pub fn show_seconds(record: DateInput, show_seconds: ShowSeconds) -> DateInput {
  DateInput(..record, show_seconds: show_seconds)
}

/// time_format sets the value of time_format for this DateInput.
///
pub fn time_format(
  record: DateInput,
  time_format: DateInputTimeFormat,
) -> DateInput {
  DateInput(..record, time_format: time_format)
}

/// min_date sets the value of min_date for this DateInput.
///
pub fn min_date(record: DateInput, min_date: Option(Date)) -> DateInput {
  DateInput(..record, min_date: min_date)
}

/// max_date sets the value of max_date for this DateInput.
///
pub fn max_date(record: DateInput, max_date: Option(Date)) -> DateInput {
  DateInput(..record, max_date: max_date)
}

/// min_time sets the value of min_time for this DateInput.
///
pub fn min_time(record: DateInput, min_time: Option(TimeParts)) -> DateInput {
  DateInput(..record, min_time: min_time)
}

/// max_time sets the value of max_time for this DateInput.
///
pub fn max_time(record: DateInput, max_time: Option(TimeParts)) -> DateInput {
  DateInput(..record, max_time: max_time)
}

/// month_label sets the value of month_label for this DateInput.
///
pub fn month_label(record: DateInput, month_label: String) -> DateInput {
  DateInput(..record, month_label: month_label)
}

/// day_label sets the value of day_label for this DateInput.
///
pub fn day_label(record: DateInput, day_label: String) -> DateInput {
  DateInput(..record, day_label: day_label)
}

/// year_label sets the value of year_label for this DateInput.
///
pub fn year_label(record: DateInput, year_label: String) -> DateInput {
  DateInput(..record, year_label: year_label)
}

/// hour_label sets the value of hour_label for this DateInput.
///
pub fn hour_label(record: DateInput, hour_label: String) -> DateInput {
  DateInput(..record, hour_label: hour_label)
}

/// minute_label sets the value of minute_label for this DateInput.
///
pub fn minute_label(record: DateInput, minute_label: String) -> DateInput {
  DateInput(..record, minute_label: minute_label)
}

/// second_label sets the value of second_label for this DateInput.
///
pub fn second_label(record: DateInput, second_label: String) -> DateInput {
  DateInput(..record, second_label: second_label)
}

/// period_label sets the value of period_label for this DateInput.
///
pub fn period_label(record: DateInput, period_label: String) -> DateInput {
  DateInput(..record, period_label: period_label)
}

/// disabled sets the value of disabled for this DateInput.
///
pub fn disabled(record: DateInput, disabled: Disabled) -> DateInput {
  DateInput(..record, disabled: disabled)
}

/// readonly sets the value of readonly for this DateInput.
///
pub fn readonly(record: DateInput, readonly: Readonly) -> DateInput {
  DateInput(..record, readonly: readonly)
}

/// required sets the value of required for this DateInput.
///
pub fn required(record: DateInput, required: Required) -> DateInput {
  DateInput(..record, required: required)
}

/// name sets the value of name for this DateInput.
///
pub fn name(record: DateInput, name: String) -> DateInput {
  DateInput(..record, name: name)
}

// --- Renderers ---

/// render creates a Lustre Element for a DateInput
///
pub fn render(
  model: DateInput,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  element.element(
    "m3e-date-input",
    list.flatten([
      [
        attr.option(
          model.value,
          fn(_) { "value" },
          date.to_string,
          default_value,
        ),
        attr.with_default(
          "type",
          date_input_type.to_string(model.type_),
          date_input_type.to_string(default_type_),
        ),
        attr.boolean("show-seconds", model.show_seconds == IsShowSeconds),
        attr.with_default(
          "time-format",
          date_input_time_format.to_string(model.time_format),
          date_input_time_format.to_string(default_time_format),
        ),
        attr.option(
          model.min_date,
          fn(_) { "min-date" },
          date.to_string,
          default_min_date,
        ),
        attr.option(
          model.max_date,
          fn(_) { "max-date" },
          date.to_string,
          default_max_date,
        ),
        attr.option(
          model.min_time,
          fn(_) { "min-time" },
          time_parts.to_string,
          default_min_time,
        ),
        attr.option(
          model.max_time,
          fn(_) { "max-time" },
          time_parts.to_string,
          default_max_time,
        ),
        attr.with_default("month-label", model.month_label, default_month_label),
        attr.with_default("day-label", model.day_label, default_day_label),
        attr.with_default("year-label", model.year_label, default_year_label),
        attr.with_default("hour-label", model.hour_label, default_hour_label),
        attr.with_default(
          "minute-label",
          model.minute_label,
          default_minute_label,
        ),
        attr.with_default(
          "second-label",
          model.second_label,
          default_second_label,
        ),
        attr.with_default(
          "period-label",
          model.period_label,
          default_period_label,
        ),
        attr.boolean("disabled", model.disabled == IsDisabled),
        attr.boolean("readonly", model.readonly == IsReadonly),
        attr.boolean("required", model.required == IsRequired),
        attr.with_default("name", model.name, default_name),
      ],
      attributes,
    ])
      |> list.filter(fn(a) { a != attribute.none() }),
    [],
  )
}

/// render_config creates a Lustre Element from a DateInput Config
///
pub fn render_config(
  c: Config,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  render(from_config(c), attributes)
}
