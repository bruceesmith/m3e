//// ThemeIcon is an icon that visually presents a preview of a theme.
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/list
import lustre/attribute.{type Attribute}
import lustre/element.{type Element}
import m3e/attr
import m3e/color_scheme.{type ColorScheme}
import m3e/theme_variant.{type ThemeVariant}

// --- Types ---

/// ThemeIcon is a View Model for this component
///
/// ## Fields:
///
/// - color: The hex color of the theme to preview
/// - scheme: The color scheme of the theme.
/// - variant: The color variant of the theme.
///
pub opaque type ThemeIcon {
  ThemeIcon(color: String, scheme: ColorScheme, variant: ThemeVariant)
}

// --- Defaults ---

pub const default_color: String = "#6750A4"

pub const default_scheme: ColorScheme = color_scheme.Auto

pub const default_variant: ThemeVariant = theme_variant.Neutral

// --- Configuration ---

/// Config is a public record for configuring this component.
///
pub type Config {
  Config(color: String, scheme: ColorScheme, variant: ThemeVariant)
}

/// default_config is the default configuration for this component.
///
pub fn default_config() -> Config {
  Config(
    color: "#6750A4",
    scheme: color_scheme.Auto,
    variant: theme_variant.Neutral,
  )
}

// --- Constructors ---

/// from_config creates a new ThemeIcon from the given configuration.
///
pub fn from_config(config: Config) -> ThemeIcon {
  ThemeIcon(color: config.color, scheme: config.scheme, variant: config.variant)
}

/// new creates a new ThemeIcon with the default configuration.
///
pub fn new() -> ThemeIcon {
  from_config(default_config())
}

// --- Setters ---

/// color sets the value of color for this ThemeIcon.
///
pub fn color(record: ThemeIcon, color: String) -> ThemeIcon {
  ThemeIcon(..record, color: color)
}

/// scheme sets the value of scheme for this ThemeIcon.
///
pub fn scheme(record: ThemeIcon, scheme: ColorScheme) -> ThemeIcon {
  ThemeIcon(..record, scheme: scheme)
}

/// variant sets the value of variant for this ThemeIcon.
///
pub fn variant(record: ThemeIcon, variant: ThemeVariant) -> ThemeIcon {
  ThemeIcon(..record, variant: variant)
}

// --- Renderers ---

/// render creates a Lustre Element for a ThemeIcon
///
pub fn render(
  model: ThemeIcon,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  element.element(
    "m3e-theme-icon",
    list.flatten([
      [
        attr.with_default("color", model.color, default_color),
        attr.with_default(
          "scheme",
          color_scheme.to_string(model.scheme),
          color_scheme.to_string(default_scheme),
        ),
        attr.with_default(
          "variant",
          theme_variant.to_string(model.variant),
          theme_variant.to_string(default_variant),
        ),
      ],
      attributes,
    ])
      |> list.filter(fn(a) { a != attribute.none() }),
    [],
  )
}

/// render_config creates a Lustre Element from a ThemeIcon Config
///
pub fn render_config(
  c: Config,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  render(from_config(c), attributes)
}
