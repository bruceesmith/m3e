//// ScaleGesture is a non-visual element used to detect a scale gesture for an associated element.
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/float
import gleam/function
import gleam/list
import gleam/option.{type Option, None}
import lustre/attribute.{type Attribute}
import lustre/element.{type Element}
import m3e/attr
import m3e/gesture_input_button.{type GestureInputButton}
import m3e/pointer_type.{type PointerType}

// --- Types ---

/// ScaleGesture is a View Model for this component
///
/// ## Fields:
///
/// - for: The identifier of the interactive control to which this element is attached.
/// - buttons: Which buttons can be pressed.
/// - pointer_types: Which types of pointers can be used to recognize gestures.
/// - disabled: Whether gesture recognition is disabled.
/// - priority: The priority in which to recognize gestures.
/// - pointers: Number of pointers that must be pressed before the gesture fails.
/// - distance_threshold: Minimum distance change (px) required to activate scale.
///
pub opaque type ScaleGesture {
  ScaleGesture(
    for: Option(String),
    buttons: List(GestureInputButton),
    pointer_types: List(PointerType),
    disabled: Disabled,
    priority: Float,
    pointers: Float,
    distance_threshold: Float,
  )
}

/// Disabled is whether gesture recognition is disabled.
///
pub type Disabled {
  IsDisabled
  IsNotDisabled
}

// --- Defaults ---

pub const default_for: Option(String) = None

pub const default_buttons: List(GestureInputButton) = [
  gesture_input_button.Primary,
]

pub const default_pointer_types: List(PointerType) = [
  pointer_type.Mouse,
  pointer_type.Pen,
  pointer_type.Touch,
]

pub const default_disabled: Disabled = IsNotDisabled

pub const default_priority: Float = 1.0

pub const default_pointers: Float = 2.0

pub const default_distance_threshold: Float = 4.0

// --- Configuration ---

/// Config is a public record for configuring this component.
///
pub type Config {
  Config(
    for: Option(String),
    buttons: List(GestureInputButton),
    pointer_types: List(PointerType),
    disabled: Disabled,
    priority: Float,
    pointers: Float,
    distance_threshold: Float,
  )
}

/// default_config is the default configuration for this component.
///
pub fn default_config() -> Config {
  Config(
    for: None,
    buttons: [gesture_input_button.Primary],
    pointer_types: [pointer_type.Mouse, pointer_type.Pen, pointer_type.Touch],
    disabled: IsNotDisabled,
    priority: 1.0,
    pointers: 2.0,
    distance_threshold: 4.0,
  )
}

// --- Constructors ---

/// from_config creates a new ScaleGesture from the given configuration.
///
pub fn from_config(config: Config) -> ScaleGesture {
  ScaleGesture(
    for: config.for,
    buttons: config.buttons,
    pointer_types: config.pointer_types,
    disabled: config.disabled,
    priority: config.priority,
    pointers: config.pointers,
    distance_threshold: config.distance_threshold,
  )
}

/// new creates a new ScaleGesture with the default configuration.
///
pub fn new() -> ScaleGesture {
  from_config(default_config())
}

// --- Setters ---

/// for sets the value of for for this ScaleGesture.
///
pub fn for(record: ScaleGesture, for: Option(String)) -> ScaleGesture {
  ScaleGesture(..record, for: for)
}

/// buttons sets the value of buttons for this ScaleGesture.
///
pub fn buttons(
  record: ScaleGesture,
  buttons: List(GestureInputButton),
) -> ScaleGesture {
  ScaleGesture(..record, buttons: buttons)
}

/// pointer_types sets the value of pointer_types for this ScaleGesture.
///
pub fn pointer_types(
  record: ScaleGesture,
  pointer_types: List(PointerType),
) -> ScaleGesture {
  ScaleGesture(..record, pointer_types: pointer_types)
}

/// disabled sets the value of disabled for this ScaleGesture.
///
pub fn disabled(record: ScaleGesture, disabled: Disabled) -> ScaleGesture {
  ScaleGesture(..record, disabled: disabled)
}

/// priority sets the value of priority for this ScaleGesture.
///
pub fn priority(record: ScaleGesture, priority: Float) -> ScaleGesture {
  ScaleGesture(..record, priority: priority)
}

/// pointers sets the value of pointers for this ScaleGesture.
///
pub fn pointers(record: ScaleGesture, pointers: Float) -> ScaleGesture {
  ScaleGesture(..record, pointers: pointers)
}

/// distance_threshold sets the value of distance_threshold for this ScaleGesture.
///
pub fn distance_threshold(
  record: ScaleGesture,
  distance_threshold: Float,
) -> ScaleGesture {
  ScaleGesture(..record, distance_threshold: distance_threshold)
}

// --- Renderers ---

/// render creates a Lustre Element for a ScaleGesture
///
pub fn render(
  model: ScaleGesture,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  element.element(
    "m3e-scale-gesture",
    list.flatten([
      [
        attr.option(model.for, fn(_) { "for" }, function.identity, default_for),
        attr.with_default(
          "buttons",
          attr.list_to_spaced_string(
            model.buttons,
            gesture_input_button.to_string,
          ),
          attr.list_to_spaced_string(
            default_buttons,
            gesture_input_button.to_string,
          ),
        ),
        attr.with_default(
          "pointer-types",
          attr.list_to_spaced_string(
            model.pointer_types,
            pointer_type.to_string,
          ),
          attr.list_to_spaced_string(
            default_pointer_types,
            pointer_type.to_string,
          ),
        ),
        attr.boolean("disabled", model.disabled == IsDisabled),
        attr.with_default(
          "priority",
          float.to_string(model.priority),
          float.to_string(default_priority),
        ),
        attr.with_default(
          "pointers",
          float.to_string(model.pointers),
          float.to_string(default_pointers),
        ),
        attr.with_default(
          "distance-threshold",
          float.to_string(model.distance_threshold),
          float.to_string(default_distance_threshold),
        ),
      ],
      attributes,
    ])
      |> list.filter(fn(a) { a != attribute.none() }),
    [],
  )
}

/// render_config creates a Lustre Element from a ScaleGesture Config
///
pub fn render_config(
  c: Config,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  render(from_config(c), attributes)
}
