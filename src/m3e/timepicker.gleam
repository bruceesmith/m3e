//// Timepicker is presents a time picker on a temporary surface.
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/function
import gleam/list
import gleam/option.{type Option, None}
import lustre/attribute.{type Attribute}
import lustre/element.{type Element}
import m3e/attr
import m3e/date.{type Date}
import m3e/time_parts.{type TimeParts}
import m3e/timepicker_format.{type TimepickerFormat}
import m3e/timepicker_mode.{type TimepickerMode}
import m3e/timepicker_orientation.{type TimepickerOrientation}
import m3e/timepicker_variant.{type TimepickerVariant}

// --- Types ---

/// Timepicker is a View Model for this component
///
/// ## Fields:
///
/// - variant: The appearance variant of the picker.
/// - mode: The mode in which to select time.
/// - orientation: The orientation of the picker.
/// - date: The selected date.
/// - format: Whether to use a 12‑hour or 24‑hour clock.
/// - max_time: The maximum time that can be selected.
/// - min_time: The minimum time that can be selected.
/// - show_seconds: Whether to show seconds.
/// - confirm_label: Label given to the button used apply the selected date and close the picker.
/// - dismiss_label: Label given to the button used discard the selected date and close the picker.
/// - dial_label: Label given to the the picker when in dial mode.
/// - input_label: Label given to the the picker when in input mode.
/// - hour_label: Label for the hour field.
/// - minute_label: Label for the minute field.
/// - second_label: The label for the second field.
/// - mode_toggle_label: The accessible label given to the mode toggle button.
/// - hide_mode_toggle: Whether to hide the mode toggle button.
/// - period_toggle_label: The accessible label given to the period toggle.
/// - for: The identifier of the interactive control to which this element is attached.
///
pub opaque type Timepicker {
  Timepicker(
    variant: TimepickerVariant,
    mode: TimepickerMode,
    orientation: TimepickerOrientation,
    date: Option(Date),
    format: TimepickerFormat,
    max_time: Option(TimeParts),
    min_time: Option(TimeParts),
    show_seconds: ShowSeconds,
    confirm_label: String,
    dismiss_label: String,
    dial_label: String,
    input_label: String,
    hour_label: String,
    minute_label: String,
    second_label: String,
    mode_toggle_label: String,
    hide_mode_toggle: HideModeToggle,
    period_toggle_label: String,
    for: Option(String),
  )
}

/// ShowSeconds is whether to show seconds.
///
pub type ShowSeconds {
  IsShowSeconds
  IsNotShowSeconds
}

/// HideModeToggle is whether to hide the mode toggle button.
///
pub type HideModeToggle {
  IsHideModeToggle
  IsNotHideModeToggle
}

// --- Defaults ---

pub const default_variant: TimepickerVariant = timepicker_variant.Docked

pub const default_mode: TimepickerMode = timepicker_mode.Dial

pub const default_orientation: TimepickerOrientation = timepicker_orientation.Vertical

pub const default_date: Option(Date) = None

pub const default_format: TimepickerFormat = timepicker_format.OneTwo

pub const default_max_time: Option(TimeParts) = None

pub const default_min_time: Option(TimeParts) = None

pub const default_show_seconds: ShowSeconds = IsNotShowSeconds

pub const default_confirm_label: String = "OK"

pub const default_dismiss_label: String = "Cancel"

pub const default_dial_label: String = "Select time"

pub const default_input_label: String = "Edit time"

pub const default_hour_label: String = "Hour"

pub const default_minute_label: String = "Minute"

pub const default_second_label: String = "Second"

pub const default_mode_toggle_label: String = "Toggle input picker"

pub const default_hide_mode_toggle: HideModeToggle = IsNotHideModeToggle

pub const default_period_toggle_label: String = "AM or PM"

pub const default_for: Option(String) = None

// --- Configuration ---

/// Config is a public record for configuring this component.
///
pub type Config {
  Config(
    variant: TimepickerVariant,
    mode: TimepickerMode,
    orientation: TimepickerOrientation,
    date: Option(Date),
    format: TimepickerFormat,
    max_time: Option(TimeParts),
    min_time: Option(TimeParts),
    show_seconds: ShowSeconds,
    confirm_label: String,
    dismiss_label: String,
    dial_label: String,
    input_label: String,
    hour_label: String,
    minute_label: String,
    second_label: String,
    mode_toggle_label: String,
    hide_mode_toggle: HideModeToggle,
    period_toggle_label: String,
    for: Option(String),
  )
}

/// default_config is the default configuration for this component.
///
pub fn default_config() -> Config {
  Config(
    variant: timepicker_variant.Docked,
    mode: timepicker_mode.Dial,
    orientation: timepicker_orientation.Vertical,
    date: None,
    format: timepicker_format.OneTwo,
    max_time: None,
    min_time: None,
    show_seconds: IsNotShowSeconds,
    confirm_label: "OK",
    dismiss_label: "Cancel",
    dial_label: "Select time",
    input_label: "Edit time",
    hour_label: "Hour",
    minute_label: "Minute",
    second_label: "Second",
    mode_toggle_label: "Toggle input picker",
    hide_mode_toggle: IsNotHideModeToggle,
    period_toggle_label: "AM or PM",
    for: None,
  )
}

// --- Constructors ---

/// from_config creates a new Timepicker from the given configuration.
///
pub fn from_config(config: Config) -> Timepicker {
  Timepicker(
    variant: config.variant,
    mode: config.mode,
    orientation: config.orientation,
    date: config.date,
    format: config.format,
    max_time: config.max_time,
    min_time: config.min_time,
    show_seconds: config.show_seconds,
    confirm_label: config.confirm_label,
    dismiss_label: config.dismiss_label,
    dial_label: config.dial_label,
    input_label: config.input_label,
    hour_label: config.hour_label,
    minute_label: config.minute_label,
    second_label: config.second_label,
    mode_toggle_label: config.mode_toggle_label,
    hide_mode_toggle: config.hide_mode_toggle,
    period_toggle_label: config.period_toggle_label,
    for: config.for,
  )
}

/// new creates a new Timepicker with the default configuration.
///
pub fn new() -> Timepicker {
  from_config(default_config())
}

// --- Setters ---

/// variant sets the value of variant for this Timepicker.
///
pub fn variant(record: Timepicker, variant: TimepickerVariant) -> Timepicker {
  Timepicker(..record, variant: variant)
}

/// mode sets the value of mode for this Timepicker.
///
pub fn mode(record: Timepicker, mode: TimepickerMode) -> Timepicker {
  Timepicker(..record, mode: mode)
}

/// orientation sets the value of orientation for this Timepicker.
///
pub fn orientation(
  record: Timepicker,
  orientation: TimepickerOrientation,
) -> Timepicker {
  Timepicker(..record, orientation: orientation)
}

/// date sets the value of date for this Timepicker.
///
pub fn date(record: Timepicker, date: Option(Date)) -> Timepicker {
  Timepicker(..record, date: date)
}

/// format sets the value of format for this Timepicker.
///
pub fn format(record: Timepicker, format: TimepickerFormat) -> Timepicker {
  Timepicker(..record, format: format)
}

/// max_time sets the value of max_time for this Timepicker.
///
pub fn max_time(record: Timepicker, max_time: Option(TimeParts)) -> Timepicker {
  Timepicker(..record, max_time: max_time)
}

/// min_time sets the value of min_time for this Timepicker.
///
pub fn min_time(record: Timepicker, min_time: Option(TimeParts)) -> Timepicker {
  Timepicker(..record, min_time: min_time)
}

/// show_seconds sets the value of show_seconds for this Timepicker.
///
pub fn show_seconds(
  record: Timepicker,
  show_seconds: ShowSeconds,
) -> Timepicker {
  Timepicker(..record, show_seconds: show_seconds)
}

/// confirm_label sets the value of confirm_label for this Timepicker.
///
pub fn confirm_label(record: Timepicker, confirm_label: String) -> Timepicker {
  Timepicker(..record, confirm_label: confirm_label)
}

/// dismiss_label sets the value of dismiss_label for this Timepicker.
///
pub fn dismiss_label(record: Timepicker, dismiss_label: String) -> Timepicker {
  Timepicker(..record, dismiss_label: dismiss_label)
}

/// dial_label sets the value of dial_label for this Timepicker.
///
pub fn dial_label(record: Timepicker, dial_label: String) -> Timepicker {
  Timepicker(..record, dial_label: dial_label)
}

/// input_label sets the value of input_label for this Timepicker.
///
pub fn input_label(record: Timepicker, input_label: String) -> Timepicker {
  Timepicker(..record, input_label: input_label)
}

/// hour_label sets the value of hour_label for this Timepicker.
///
pub fn hour_label(record: Timepicker, hour_label: String) -> Timepicker {
  Timepicker(..record, hour_label: hour_label)
}

/// minute_label sets the value of minute_label for this Timepicker.
///
pub fn minute_label(record: Timepicker, minute_label: String) -> Timepicker {
  Timepicker(..record, minute_label: minute_label)
}

/// second_label sets the value of second_label for this Timepicker.
///
pub fn second_label(record: Timepicker, second_label: String) -> Timepicker {
  Timepicker(..record, second_label: second_label)
}

/// mode_toggle_label sets the value of mode_toggle_label for this Timepicker.
///
pub fn mode_toggle_label(
  record: Timepicker,
  mode_toggle_label: String,
) -> Timepicker {
  Timepicker(..record, mode_toggle_label: mode_toggle_label)
}

/// hide_mode_toggle sets the value of hide_mode_toggle for this Timepicker.
///
pub fn hide_mode_toggle(
  record: Timepicker,
  hide_mode_toggle: HideModeToggle,
) -> Timepicker {
  Timepicker(..record, hide_mode_toggle: hide_mode_toggle)
}

/// period_toggle_label sets the value of period_toggle_label for this Timepicker.
///
pub fn period_toggle_label(
  record: Timepicker,
  period_toggle_label: String,
) -> Timepicker {
  Timepicker(..record, period_toggle_label: period_toggle_label)
}

/// for sets the value of for for this Timepicker.
///
pub fn for(record: Timepicker, for: Option(String)) -> Timepicker {
  Timepicker(..record, for: for)
}

// --- Renderers ---

/// render creates a Lustre Element for a Timepicker
///
pub fn render(
  model: Timepicker,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  element.element(
    "m3e-timepicker",
    list.flatten([
      [
        attr.with_default(
          "variant",
          timepicker_variant.to_string(model.variant),
          timepicker_variant.to_string(default_variant),
        ),
        attr.with_default(
          "mode",
          timepicker_mode.to_string(model.mode),
          timepicker_mode.to_string(default_mode),
        ),
        attr.with_default(
          "orientation",
          timepicker_orientation.to_string(model.orientation),
          timepicker_orientation.to_string(default_orientation),
        ),
        attr.option(model.date, fn(_) { "date" }, date.to_string, default_date),
        attr.with_default(
          "format",
          timepicker_format.to_string(model.format),
          timepicker_format.to_string(default_format),
        ),
        attr.option(
          model.max_time,
          fn(_) { "max-time" },
          time_parts.to_string,
          default_max_time,
        ),
        attr.option(
          model.min_time,
          fn(_) { "min-time" },
          time_parts.to_string,
          default_min_time,
        ),
        attr.boolean("show-seconds", model.show_seconds == IsShowSeconds),
        attr.with_default(
          "confirm-label",
          model.confirm_label,
          default_confirm_label,
        ),
        attr.with_default(
          "dismiss-label",
          model.dismiss_label,
          default_dismiss_label,
        ),
        attr.with_default("dial-label", model.dial_label, default_dial_label),
        attr.with_default("input-label", model.input_label, default_input_label),
        attr.with_default("hour-label", model.hour_label, default_hour_label),
        attr.with_default(
          "minute-label",
          model.minute_label,
          default_minute_label,
        ),
        attr.with_default(
          "second-label",
          model.second_label,
          default_second_label,
        ),
        attr.with_default(
          "mode-toggle-label",
          model.mode_toggle_label,
          default_mode_toggle_label,
        ),
        attr.boolean(
          "hide-mode-toggle",
          model.hide_mode_toggle == IsHideModeToggle,
        ),
        attr.with_default(
          "period-toggle-label",
          model.period_toggle_label,
          default_period_toggle_label,
        ),
        attr.option(model.for, fn(_) { "for" }, function.identity, default_for),
      ],
      attributes,
    ])
      |> list.filter(fn(a) { a != attribute.none() }),
    [],
  )
}

/// render_config creates a Lustre Element from a Timepicker Config
///
pub fn render_config(
  c: Config,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  render(from_config(c), attributes)
}
