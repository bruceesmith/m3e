//// TapGesture is a non-visual element used to detect a tap gesture for an associated element.
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/float
import gleam/function
import gleam/list
import gleam/option.{type Option, None}
import lustre/attribute.{type Attribute}
import lustre/element.{type Element}
import m3e/attr
import m3e/gesture_input_button.{type GestureInputButton}

// --- Types ---

/// TapGesture is a View Model for this component
///
/// ## Fields:
///
/// - allowed_buttons: Which buttons can be pressed.
/// - disabled: Whether gesture recognition is disabled.
/// - priority: The priority in which to recognize gestures.
/// - pointers: Number of pointers that must be pressed before the gesture fails.
/// - max_press_interval: Maximum time (ms) between tap presses.
/// - max_release_interval: Maximum time (ms) between tap releases.
/// - max_displacement: Maximum distance (px) a pointer can move before the gesture fails.
/// - max_duration: Maximum time (ms) taps can be pressed before the gesture fails.
/// - for: The identifier of the interactive control to which this element is attached.
///
pub opaque type TapGesture {
  TapGesture(
    allowed_buttons: GestureInputButton,
    disabled: Disabled,
    priority: Float,
    pointers: Float,
    max_press_interval: Float,
    max_release_interval: Float,
    max_displacement: Float,
    max_duration: Float,
    for: Option(String),
  )
}

/// Disabled is whether gesture recognition is disabled.
///
pub type Disabled {
  IsDisabled
  IsNotDisabled
}

// --- Defaults ---

pub const default_allowed_buttons: GestureInputButton = gesture_input_button.Primary

pub const default_disabled: Disabled = IsNotDisabled

pub const default_priority: Float = 1.0

pub const default_pointers: Float = 1.0

pub const default_max_press_interval: Float = 120.0

pub const default_max_release_interval: Float = 120.0

pub const default_max_displacement: Float = 12.0

pub const default_max_duration: Float = 180.0

pub const default_for: Option(String) = None

// --- Configuration ---

/// Config is a public record for configuring this component.
///
pub type Config {
  Config(
    allowed_buttons: GestureInputButton,
    disabled: Disabled,
    priority: Float,
    pointers: Float,
    max_press_interval: Float,
    max_release_interval: Float,
    max_displacement: Float,
    max_duration: Float,
    for: Option(String),
  )
}

/// default_config is the default configuration for this component.
///
pub fn default_config() -> Config {
  Config(
    allowed_buttons: gesture_input_button.Primary,
    disabled: IsNotDisabled,
    priority: 1.0,
    pointers: 1.0,
    max_press_interval: 120.0,
    max_release_interval: 120.0,
    max_displacement: 12.0,
    max_duration: 180.0,
    for: None,
  )
}

// --- Constructors ---

/// from_config creates a new TapGesture from the given configuration.
///
pub fn from_config(config: Config) -> TapGesture {
  TapGesture(
    allowed_buttons: config.allowed_buttons,
    disabled: config.disabled,
    priority: config.priority,
    pointers: config.pointers,
    max_press_interval: config.max_press_interval,
    max_release_interval: config.max_release_interval,
    max_displacement: config.max_displacement,
    max_duration: config.max_duration,
    for: config.for,
  )
}

/// new creates a new TapGesture with the default configuration.
///
pub fn new() -> TapGesture {
  from_config(default_config())
}

// --- Setters ---

/// allowed_buttons sets the value of allowed_buttons for this TapGesture.
///
pub fn allowed_buttons(
  record: TapGesture,
  allowed_buttons: GestureInputButton,
) -> TapGesture {
  TapGesture(..record, allowed_buttons: allowed_buttons)
}

/// disabled sets the value of disabled for this TapGesture.
///
pub fn disabled(record: TapGesture, disabled: Disabled) -> TapGesture {
  TapGesture(..record, disabled: disabled)
}

/// priority sets the value of priority for this TapGesture.
///
pub fn priority(record: TapGesture, priority: Float) -> TapGesture {
  TapGesture(..record, priority: priority)
}

/// pointers sets the value of pointers for this TapGesture.
///
pub fn pointers(record: TapGesture, pointers: Float) -> TapGesture {
  TapGesture(..record, pointers: pointers)
}

/// max_press_interval sets the value of max_press_interval for this TapGesture.
///
pub fn max_press_interval(
  record: TapGesture,
  max_press_interval: Float,
) -> TapGesture {
  TapGesture(..record, max_press_interval: max_press_interval)
}

/// max_release_interval sets the value of max_release_interval for this TapGesture.
///
pub fn max_release_interval(
  record: TapGesture,
  max_release_interval: Float,
) -> TapGesture {
  TapGesture(..record, max_release_interval: max_release_interval)
}

/// max_displacement sets the value of max_displacement for this TapGesture.
///
pub fn max_displacement(
  record: TapGesture,
  max_displacement: Float,
) -> TapGesture {
  TapGesture(..record, max_displacement: max_displacement)
}

/// max_duration sets the value of max_duration for this TapGesture.
///
pub fn max_duration(record: TapGesture, max_duration: Float) -> TapGesture {
  TapGesture(..record, max_duration: max_duration)
}

/// for sets the value of for for this TapGesture.
///
pub fn for(record: TapGesture, for: Option(String)) -> TapGesture {
  TapGesture(..record, for: for)
}

// --- Renderers ---

/// render creates a Lustre Element for a TapGesture
///
pub fn render(
  model: TapGesture,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  element.element(
    "m3e-tap-gesture",
    list.flatten([
      [
        attr.with_default(
          "allowed-buttons",
          gesture_input_button.to_string(model.allowed_buttons),
          gesture_input_button.to_string(default_allowed_buttons),
        ),
        attr.boolean("disabled", model.disabled == IsDisabled),
        attr.with_default(
          "priority",
          float.to_string(model.priority),
          float.to_string(default_priority),
        ),
        attr.with_default(
          "pointers",
          float.to_string(model.pointers),
          float.to_string(default_pointers),
        ),
        attr.with_default(
          "max-press-interval",
          float.to_string(model.max_press_interval),
          float.to_string(default_max_press_interval),
        ),
        attr.with_default(
          "max-release-interval",
          float.to_string(model.max_release_interval),
          float.to_string(default_max_release_interval),
        ),
        attr.with_default(
          "max-displacement",
          float.to_string(model.max_displacement),
          float.to_string(default_max_displacement),
        ),
        attr.with_default(
          "max-duration",
          float.to_string(model.max_duration),
          float.to_string(default_max_duration),
        ),
        attr.option(model.for, fn(_) { "for" }, function.identity, default_for),
      ],
      attributes,
    ])
      |> list.filter(fn(a) { a != attribute.none() }),
    [],
  )
}

/// render_config creates a Lustre Element from a TapGesture Config
///
pub fn render_config(
  c: Config,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  render(from_config(c), attributes)
}
