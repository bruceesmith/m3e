//// RotateGesture is a non-visual element used to detect and interpret rotate gestures from an input stream
//// //// produced from an attached element.
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/float
import gleam/function
import gleam/list
import gleam/option.{type Option, None}
import lustre/attribute.{type Attribute}
import lustre/element.{type Element}
import m3e/attr
import m3e/gesture_input_button.{type GestureInputButton}
import m3e/pan_gesture_activation_mode.{type PanGestureActivationMode}
import m3e/pointer_type.{type PointerType}

// --- Types ---

/// RotateGesture is a View Model for this component
///
/// ## Fields:
///
/// - for: The identifier of the interactive control to which this element is attached.
/// - buttons: Which buttons can be pressed.
/// - pointer_types: Which types of pointers can be used to recognize gestures.
/// - disabled: Whether gesture recognition is disabled.
/// - priority: The priority in which to recognize gestures.
/// - activation_mode: Mode in which to activate the gesture.
/// - pointers: Number of pointers required for the gesture to be recognized.
/// - min_displacement: Minimum centroid displacement (px) before rotation starts.
/// - max_press_interval: Maximum allowed time (ms) between the earliest and latest press.
///
pub opaque type RotateGesture {
  RotateGesture(
    for: Option(String),
    buttons: List(GestureInputButton),
    pointer_types: List(PointerType),
    disabled: Disabled,
    priority: String,
    activation_mode: PanGestureActivationMode,
    pointers: Float,
    min_displacement: Float,
    max_press_interval: Float,
  )
}

/// Disabled is whether gesture recognition is disabled.
///
pub type Disabled {
  IsDisabled
  IsNotDisabled
}

// --- Defaults ---

pub const default_for: Option(String) = None

pub const default_buttons: List(GestureInputButton) = [
  gesture_input_button.Primary,
]

pub const default_pointer_types: List(PointerType) = [
  pointer_type.Mouse,
  pointer_type.Pen,
  pointer_type.Touch,
]

pub const default_disabled: Disabled = IsNotDisabled

pub const default_priority: String = "1"

pub const default_activation_mode: PanGestureActivationMode = pan_gesture_activation_mode.Press

pub const default_pointers: Float = 2.0

pub const default_min_displacement: Float = 4.0

pub const default_max_press_interval: Float = 120.0

// --- Configuration ---

/// Config is a public record for configuring this component.
///
pub type Config {
  Config(
    for: Option(String),
    buttons: List(GestureInputButton),
    pointer_types: List(PointerType),
    disabled: Disabled,
    priority: String,
    activation_mode: PanGestureActivationMode,
    pointers: Float,
    min_displacement: Float,
    max_press_interval: Float,
  )
}

/// default_config is the default configuration for this component.
///
pub fn default_config() -> Config {
  Config(
    for: None,
    buttons: [gesture_input_button.Primary],
    pointer_types: [pointer_type.Mouse, pointer_type.Pen, pointer_type.Touch],
    disabled: IsNotDisabled,
    priority: "1",
    activation_mode: pan_gesture_activation_mode.Press,
    pointers: 2.0,
    min_displacement: 4.0,
    max_press_interval: 120.0,
  )
}

// --- Constructors ---

/// from_config creates a new RotateGesture from the given configuration.
///
pub fn from_config(config: Config) -> RotateGesture {
  RotateGesture(
    for: config.for,
    buttons: config.buttons,
    pointer_types: config.pointer_types,
    disabled: config.disabled,
    priority: config.priority,
    activation_mode: config.activation_mode,
    pointers: config.pointers,
    min_displacement: config.min_displacement,
    max_press_interval: config.max_press_interval,
  )
}

/// new creates a new RotateGesture with the default configuration.
///
pub fn new() -> RotateGesture {
  from_config(default_config())
}

// --- Setters ---

/// for sets the value of for for this RotateGesture.
///
pub fn for(record: RotateGesture, for: Option(String)) -> RotateGesture {
  RotateGesture(..record, for: for)
}

/// buttons sets the value of buttons for this RotateGesture.
///
pub fn buttons(
  record: RotateGesture,
  buttons: List(GestureInputButton),
) -> RotateGesture {
  RotateGesture(..record, buttons: buttons)
}

/// pointer_types sets the value of pointer_types for this RotateGesture.
///
pub fn pointer_types(
  record: RotateGesture,
  pointer_types: List(PointerType),
) -> RotateGesture {
  RotateGesture(..record, pointer_types: pointer_types)
}

/// disabled sets the value of disabled for this RotateGesture.
///
pub fn disabled(record: RotateGesture, disabled: Disabled) -> RotateGesture {
  RotateGesture(..record, disabled: disabled)
}

/// priority sets the value of priority for this RotateGesture.
///
pub fn priority(record: RotateGesture, priority: String) -> RotateGesture {
  RotateGesture(..record, priority: priority)
}

/// activation_mode sets the value of activation_mode for this RotateGesture.
///
pub fn activation_mode(
  record: RotateGesture,
  activation_mode: PanGestureActivationMode,
) -> RotateGesture {
  RotateGesture(..record, activation_mode: activation_mode)
}

/// pointers sets the value of pointers for this RotateGesture.
///
pub fn pointers(record: RotateGesture, pointers: Float) -> RotateGesture {
  RotateGesture(..record, pointers: pointers)
}

/// min_displacement sets the value of min_displacement for this RotateGesture.
///
pub fn min_displacement(
  record: RotateGesture,
  min_displacement: Float,
) -> RotateGesture {
  RotateGesture(..record, min_displacement: min_displacement)
}

/// max_press_interval sets the value of max_press_interval for this RotateGesture.
///
pub fn max_press_interval(
  record: RotateGesture,
  max_press_interval: Float,
) -> RotateGesture {
  RotateGesture(..record, max_press_interval: max_press_interval)
}

// --- Renderers ---

/// render creates a Lustre Element for a RotateGesture
///
pub fn render(
  model: RotateGesture,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  element.element(
    "m3e-rotate-gesture",
    list.flatten([
      [
        attr.option(model.for, fn(_) { "for" }, function.identity, default_for),
        attr.with_default(
          "buttons",
          attr.list_to_spaced_string(
            model.buttons,
            gesture_input_button.to_string,
          ),
          attr.list_to_spaced_string(
            default_buttons,
            gesture_input_button.to_string,
          ),
        ),
        attr.with_default(
          "pointer-types",
          attr.list_to_spaced_string(
            model.pointer_types,
            pointer_type.to_string,
          ),
          attr.list_to_spaced_string(
            default_pointer_types,
            pointer_type.to_string,
          ),
        ),
        attr.boolean("disabled", model.disabled == IsDisabled),
        attr.with_default("priority", model.priority, default_priority),
        attr.with_default(
          "activation-mode",
          pan_gesture_activation_mode.to_string(model.activation_mode),
          pan_gesture_activation_mode.to_string(default_activation_mode),
        ),
        attr.with_default(
          "pointers",
          float.to_string(model.pointers),
          float.to_string(default_pointers),
        ),
        attr.with_default(
          "min-displacement",
          float.to_string(model.min_displacement),
          float.to_string(default_min_displacement),
        ),
        attr.with_default(
          "max-press-interval",
          float.to_string(model.max_press_interval),
          float.to_string(default_max_press_interval),
        ),
      ],
      attributes,
    ])
      |> list.filter(fn(a) { a != attribute.none() }),
    [],
  )
}

/// render_config creates a Lustre Element from a RotateGesture Config
///
pub fn render_config(
  c: Config,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  render(from_config(c), attributes)
}
