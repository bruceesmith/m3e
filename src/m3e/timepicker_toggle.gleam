//// TimepickerToggle is an element, nested within a clickable element, used to toggle a timepicker.
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/function
import gleam/list
import gleam/option.{type Option, None}
import lustre/attribute.{type Attribute}
import lustre/element.{type Element}
import m3e/attr

// --- Types ---

/// TimepickerToggle is a View Model for this component
///
/// ## Fields:
///
/// - for: The identifier of the interactive control to which this element is attached.
///
pub opaque type TimepickerToggle {
  TimepickerToggle(for: Option(String))
}

// --- Defaults ---

pub const default_for: Option(String) = None

// --- Constructors ---

/// new creates a new TimepickerToggle with the default configuration.
///
pub fn new(for: Option(String)) -> TimepickerToggle {
  TimepickerToggle(for: for)
}

// --- Setters ---

/// for sets the value of for for this TimepickerToggle.
///
pub fn for(_: TimepickerToggle, for: Option(String)) -> TimepickerToggle {
  TimepickerToggle(for: for)
}

// --- Renderers ---

/// render creates a Lustre Element for a TimepickerToggle
///
pub fn render(
  model: TimepickerToggle,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  element.element(
    "m3e-timepicker-toggle",
    list.flatten([
      [
        attr.option(model.for, fn(_) { "for" }, function.identity, default_for),
      ],
      attributes,
    ])
      |> list.filter(fn(a) { a != attribute.none() }),
    [],
  )
}
