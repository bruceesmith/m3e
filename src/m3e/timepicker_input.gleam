//// TimepickerInput is a keyboard‑based time surface for choosing hours and minutes.
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/float
import gleam/function
import gleam/list
import gleam/option.{type Option, None}
import lustre/attribute.{type Attribute}
import lustre/element.{type Element}
import m3e/attr
import m3e/time_parts.{type TimeParts}
import m3e/timepicker_format.{type TimepickerFormat}
import m3e/timepicker_orientation.{type TimepickerOrientation}
import m3e/timepicker_period.{type TimepickerPeriod}
import m3e/timepicker_view.{type TimepickerView}

// --- Types ---

/// TimepickerInput is a View Model for this component
///
/// ## Fields:
///
/// - format: Whether to use a 12‑hour or 24‑hour clock.
/// - hide_labels: Whether to hide field labels.
/// - hour: The hour, in 24-hour time, from 0..23.
/// - max_time: The maximum time that can be selected.
/// - min_time: The minimum time that can be selected.
/// - minute: The minute, from 0..59.
/// - second: The second, from 0..59.
/// - show_seconds: Whether to show seconds.
/// - orientation: The orientation of the input.
/// - period: The 12-hour time period.
/// - view: The view used to input time.
/// - hour_label: The label for the hour field.
/// - minute_label: The label for the minute field.
/// - second_label: The label for the second field.
/// - period_toggle_label: The accessible label given to the period toggle.
/// - for: The identifier of the interactive control to which this element is attached.
///
pub opaque type TimepickerInput {
  TimepickerInput(
    format: TimepickerFormat,
    hide_labels: HideLabels,
    hour: Option(Float),
    max_time: Option(TimeParts),
    min_time: Option(TimeParts),
    minute: Option(Float),
    second: Option(Float),
    show_seconds: ShowSeconds,
    orientation: TimepickerOrientation,
    period: TimepickerPeriod,
    view: TimepickerView,
    hour_label: String,
    minute_label: String,
    second_label: String,
    period_toggle_label: String,
    for: Option(String),
  )
}

/// HideLabels is whether to hide field labels.
///
pub type HideLabels {
  IsHideLabels
  IsNotHideLabels
}

/// ShowSeconds is whether to show seconds.
///
pub type ShowSeconds {
  IsShowSeconds
  IsNotShowSeconds
}

// --- Defaults ---

pub const default_format: TimepickerFormat = timepicker_format.OneTwo

pub const default_hide_labels: HideLabels = IsNotHideLabels

pub const default_hour: Option(Float) = None

pub const default_max_time: Option(TimeParts) = None

pub const default_min_time: Option(TimeParts) = None

pub const default_minute: Option(Float) = None

pub const default_second: Option(Float) = None

pub const default_show_seconds: ShowSeconds = IsNotShowSeconds

pub const default_orientation: TimepickerOrientation = timepicker_orientation.Horizontal

pub const default_period: TimepickerPeriod = timepicker_period.Am

pub const default_view: TimepickerView = timepicker_view.Hour

pub const default_hour_label: String = "Hour"

pub const default_minute_label: String = "Minute"

pub const default_second_label: String = "Second"

pub const default_period_toggle_label: String = "AM or PM"

pub const default_for: Option(String) = None

// --- Configuration ---

/// Config is a public record for configuring this component.
///
pub type Config {
  Config(
    format: TimepickerFormat,
    hide_labels: HideLabels,
    hour: Option(Float),
    max_time: Option(TimeParts),
    min_time: Option(TimeParts),
    minute: Option(Float),
    second: Option(Float),
    show_seconds: ShowSeconds,
    orientation: TimepickerOrientation,
    period: TimepickerPeriod,
    view: TimepickerView,
    hour_label: String,
    minute_label: String,
    second_label: String,
    period_toggle_label: String,
    for: Option(String),
  )
}

/// default_config is the default configuration for this component.
///
pub fn default_config() -> Config {
  Config(
    format: timepicker_format.OneTwo,
    hide_labels: IsNotHideLabels,
    hour: None,
    max_time: None,
    min_time: None,
    minute: None,
    second: None,
    show_seconds: IsNotShowSeconds,
    orientation: timepicker_orientation.Horizontal,
    period: timepicker_period.Am,
    view: timepicker_view.Hour,
    hour_label: "Hour",
    minute_label: "Minute",
    second_label: "Second",
    period_toggle_label: "AM or PM",
    for: None,
  )
}

// --- Constructors ---

/// from_config creates a new TimepickerInput from the given configuration.
///
pub fn from_config(config: Config) -> TimepickerInput {
  TimepickerInput(
    format: config.format,
    hide_labels: config.hide_labels,
    hour: config.hour,
    max_time: config.max_time,
    min_time: config.min_time,
    minute: config.minute,
    second: config.second,
    show_seconds: config.show_seconds,
    orientation: config.orientation,
    period: config.period,
    view: config.view,
    hour_label: config.hour_label,
    minute_label: config.minute_label,
    second_label: config.second_label,
    period_toggle_label: config.period_toggle_label,
    for: config.for,
  )
}

/// new creates a new TimepickerInput with the default configuration.
///
pub fn new() -> TimepickerInput {
  from_config(default_config())
}

// --- Setters ---

/// format sets the value of format for this TimepickerInput.
///
pub fn format(
  record: TimepickerInput,
  format: TimepickerFormat,
) -> TimepickerInput {
  TimepickerInput(..record, format: format)
}

/// hide_labels sets the value of hide_labels for this TimepickerInput.
///
pub fn hide_labels(
  record: TimepickerInput,
  hide_labels: HideLabels,
) -> TimepickerInput {
  TimepickerInput(..record, hide_labels: hide_labels)
}

/// hour sets the value of hour for this TimepickerInput.
///
pub fn hour(record: TimepickerInput, hour: Option(Float)) -> TimepickerInput {
  TimepickerInput(..record, hour: hour)
}

/// max_time sets the value of max_time for this TimepickerInput.
///
pub fn max_time(
  record: TimepickerInput,
  max_time: Option(TimeParts),
) -> TimepickerInput {
  TimepickerInput(..record, max_time: max_time)
}

/// min_time sets the value of min_time for this TimepickerInput.
///
pub fn min_time(
  record: TimepickerInput,
  min_time: Option(TimeParts),
) -> TimepickerInput {
  TimepickerInput(..record, min_time: min_time)
}

/// minute sets the value of minute for this TimepickerInput.
///
pub fn minute(
  record: TimepickerInput,
  minute: Option(Float),
) -> TimepickerInput {
  TimepickerInput(..record, minute: minute)
}

/// second sets the value of second for this TimepickerInput.
///
pub fn second(
  record: TimepickerInput,
  second: Option(Float),
) -> TimepickerInput {
  TimepickerInput(..record, second: second)
}

/// show_seconds sets the value of show_seconds for this TimepickerInput.
///
pub fn show_seconds(
  record: TimepickerInput,
  show_seconds: ShowSeconds,
) -> TimepickerInput {
  TimepickerInput(..record, show_seconds: show_seconds)
}

/// orientation sets the value of orientation for this TimepickerInput.
///
pub fn orientation(
  record: TimepickerInput,
  orientation: TimepickerOrientation,
) -> TimepickerInput {
  TimepickerInput(..record, orientation: orientation)
}

/// period sets the value of period for this TimepickerInput.
///
pub fn period(
  record: TimepickerInput,
  period: TimepickerPeriod,
) -> TimepickerInput {
  TimepickerInput(..record, period: period)
}

/// view sets the value of view for this TimepickerInput.
///
pub fn view(record: TimepickerInput, view: TimepickerView) -> TimepickerInput {
  TimepickerInput(..record, view: view)
}

/// hour_label sets the value of hour_label for this TimepickerInput.
///
pub fn hour_label(
  record: TimepickerInput,
  hour_label: String,
) -> TimepickerInput {
  TimepickerInput(..record, hour_label: hour_label)
}

/// minute_label sets the value of minute_label for this TimepickerInput.
///
pub fn minute_label(
  record: TimepickerInput,
  minute_label: String,
) -> TimepickerInput {
  TimepickerInput(..record, minute_label: minute_label)
}

/// second_label sets the value of second_label for this TimepickerInput.
///
pub fn second_label(
  record: TimepickerInput,
  second_label: String,
) -> TimepickerInput {
  TimepickerInput(..record, second_label: second_label)
}

/// period_toggle_label sets the value of period_toggle_label for this TimepickerInput.
///
pub fn period_toggle_label(
  record: TimepickerInput,
  period_toggle_label: String,
) -> TimepickerInput {
  TimepickerInput(..record, period_toggle_label: period_toggle_label)
}

/// for sets the value of for for this TimepickerInput.
///
pub fn for(record: TimepickerInput, for: Option(String)) -> TimepickerInput {
  TimepickerInput(..record, for: for)
}

// --- Renderers ---

/// render creates a Lustre Element for a TimepickerInput
///
pub fn render(
  model: TimepickerInput,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  element.element(
    "m3e-timepicker-input",
    list.flatten([
      [
        attr.with_default(
          "format",
          timepicker_format.to_string(model.format),
          timepicker_format.to_string(default_format),
        ),
        attr.boolean("hide-labels", model.hide_labels == IsHideLabels),
        attr.option(model.hour, fn(_) { "hour" }, float.to_string, default_hour),
        attr.option(
          model.max_time,
          fn(_) { "max-time" },
          time_parts.to_string,
          default_max_time,
        ),
        attr.option(
          model.min_time,
          fn(_) { "min-time" },
          time_parts.to_string,
          default_min_time,
        ),
        attr.option(
          model.minute,
          fn(_) { "minute" },
          float.to_string,
          default_minute,
        ),
        attr.option(
          model.second,
          fn(_) { "second" },
          float.to_string,
          default_second,
        ),
        attr.boolean("show-seconds", model.show_seconds == IsShowSeconds),
        attr.with_default(
          "orientation",
          timepicker_orientation.to_string(model.orientation),
          timepicker_orientation.to_string(default_orientation),
        ),
        attr.with_default(
          "period",
          timepicker_period.to_string(model.period),
          timepicker_period.to_string(default_period),
        ),
        attr.with_default(
          "view",
          timepicker_view.to_string(model.view),
          timepicker_view.to_string(default_view),
        ),
        attr.with_default("hour-label", model.hour_label, default_hour_label),
        attr.with_default(
          "minute-label",
          model.minute_label,
          default_minute_label,
        ),
        attr.with_default(
          "second-label",
          model.second_label,
          default_second_label,
        ),
        attr.with_default(
          "period-toggle-label",
          model.period_toggle_label,
          default_period_toggle_label,
        ),
        attr.option(model.for, fn(_) { "for" }, function.identity, default_for),
      ],
      attributes,
    ])
      |> list.filter(fn(a) { a != attribute.none() }),
    [],
  )
}

/// render_config creates a Lustre Element from a TimepickerInput Config
///
pub fn render_config(
  c: Config,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  render(from_config(c), attributes)
}
