//// SelectionIndicator is provides selection, focus, and hover state layer treatment for an interactive element that supports selection.
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/function
import gleam/list
import gleam/option.{type Option, None}
import lustre/attribute.{type Attribute}
import lustre/element.{type Element}
import m3e/attr

// --- Types ---

/// SelectionIndicator is a View Model for this component
///
/// ## Fields:
///
/// - bounce: Whether the indicator presents a bounce animation when selected.
/// - centered: Whether the selection animation always originates from the center of the element's bounds,
///     rather than originating from the location of the click event.
/// - disabled: Whether hover and focus events will not trigger the indicator's state layer. State layers can still
///     be controlled manually using the `show` and `hide` methods.
/// - selected: Whether the indicator is selected.
/// - for: The identifier of the interactive control to which this element is attached.
///
pub opaque type SelectionIndicator {
  SelectionIndicator(
    bounce: Bounce,
    centered: Centered,
    disabled: Disabled,
    selected: Selected,
    for: Option(String),
  )
}

/// Bounce is whether the indicator presents a bounce animation when selected.
///
pub type Bounce {
  IsBounce
  IsNotBounce
}

/// Centered is whether the selection animation always originates from the center of the element's bounds,
///     rather than originating from the location of the click event.
///
pub type Centered {
  IsCentered
  IsNotCentered
}

/// Disabled is whether hover and focus events will not trigger the indicator's state layer. State layers can still
///     be controlled manually using the `show` and `hide` methods.
///
pub type Disabled {
  IsDisabled
  IsNotDisabled
}

/// Selected is whether the indicator is selected.
///
pub type Selected {
  IsSelected
  IsNotSelected
}

// --- Defaults ---

pub const default_bounce: Bounce = IsNotBounce

pub const default_centered: Centered = IsNotCentered

pub const default_disabled: Disabled = IsNotDisabled

pub const default_selected: Selected = IsNotSelected

pub const default_for: Option(String) = None

// --- Configuration ---

/// Config is a public record for configuring this component.
///
pub type Config {
  Config(
    bounce: Bounce,
    centered: Centered,
    disabled: Disabled,
    selected: Selected,
    for: Option(String),
  )
}

/// default_config is the default configuration for this component.
///
pub fn default_config() -> Config {
  Config(
    bounce: IsNotBounce,
    centered: IsNotCentered,
    disabled: IsNotDisabled,
    selected: IsNotSelected,
    for: None,
  )
}

// --- Constructors ---

/// from_config creates a new SelectionIndicator from the given configuration.
///
pub fn from_config(config: Config) -> SelectionIndicator {
  SelectionIndicator(
    bounce: config.bounce,
    centered: config.centered,
    disabled: config.disabled,
    selected: config.selected,
    for: config.for,
  )
}

/// new creates a new SelectionIndicator with the default configuration.
///
pub fn new() -> SelectionIndicator {
  from_config(default_config())
}

// --- Setters ---

/// bounce sets the value of bounce for this SelectionIndicator.
///
pub fn bounce(
  record: SelectionIndicator,
  bounce: Bounce,
) -> SelectionIndicator {
  SelectionIndicator(..record, bounce: bounce)
}

/// centered sets the value of centered for this SelectionIndicator.
///
pub fn centered(
  record: SelectionIndicator,
  centered: Centered,
) -> SelectionIndicator {
  SelectionIndicator(..record, centered: centered)
}

/// disabled sets the value of disabled for this SelectionIndicator.
///
pub fn disabled(
  record: SelectionIndicator,
  disabled: Disabled,
) -> SelectionIndicator {
  SelectionIndicator(..record, disabled: disabled)
}

/// selected sets the value of selected for this SelectionIndicator.
///
pub fn selected(
  record: SelectionIndicator,
  selected: Selected,
) -> SelectionIndicator {
  SelectionIndicator(..record, selected: selected)
}

/// for sets the value of for for this SelectionIndicator.
///
pub fn for(
  record: SelectionIndicator,
  for: Option(String),
) -> SelectionIndicator {
  SelectionIndicator(..record, for: for)
}

// --- Renderers ---

/// render creates a Lustre Element for a SelectionIndicator
///
pub fn render(
  model: SelectionIndicator,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  element.element(
    "m3e-selection-indicator",
    list.flatten([
      [
        attr.boolean("bounce", model.bounce == IsBounce),
        attr.boolean("centered", model.centered == IsCentered),
        attr.boolean("disabled", model.disabled == IsDisabled),
        attr.boolean("selected", model.selected == IsSelected),
        attr.option(model.for, fn(_) { "for" }, function.identity, default_for),
      ],
      attributes,
    ])
      |> list.filter(fn(a) { a != attribute.none() }),
    [],
  )
}

/// render_config creates a Lustre Element from a SelectionIndicator Config
///
pub fn render_config(
  c: Config,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  render(from_config(c), attributes)
}
