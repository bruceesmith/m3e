//// TimepickerDial is a clock‑face surface for selecting hours and minutes using a movable hand.
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/float
import gleam/list
import gleam/option.{type Option, None}
import lustre/attribute.{type Attribute}
import lustre/element.{type Element}
import m3e/attr
import m3e/time_parts.{type TimeParts}
import m3e/timepicker_format.{type TimepickerFormat}
import m3e/timepicker_period.{type TimepickerPeriod}
import m3e/timepicker_view.{type TimepickerView}

// --- Types ---

/// TimepickerDial is a View Model for this component
///
/// ## Fields:
///
/// - format: Whether to use a 12‑hour or 24‑hour clock.
/// - hour: The hour, in 24-hour time, from 0..23.
/// - max_time: The maximum time that can be selected.
/// - min_time: The minimum time that can be selected.
/// - minute: The minute, from 0..59.
/// - second: The second, from 0..59.
/// - show_seconds: Whether to show seconds.
/// - period: The 12-hour time period.
/// - view: The view used to input time.
///
pub opaque type TimepickerDial {
  TimepickerDial(
    format: TimepickerFormat,
    hour: Option(Float),
    max_time: Option(TimeParts),
    min_time: Option(TimeParts),
    minute: Option(Float),
    second: Option(Float),
    show_seconds: ShowSeconds,
    period: TimepickerPeriod,
    view: TimepickerView,
  )
}

/// ShowSeconds is whether to show seconds.
///
pub type ShowSeconds {
  IsShowSeconds
  IsNotShowSeconds
}

// --- Defaults ---

pub const default_format: TimepickerFormat = timepicker_format.OneTwo

pub const default_hour: Option(Float) = None

pub const default_max_time: Option(TimeParts) = None

pub const default_min_time: Option(TimeParts) = None

pub const default_minute: Option(Float) = None

pub const default_second: Option(Float) = None

pub const default_show_seconds: ShowSeconds = IsNotShowSeconds

pub const default_period: TimepickerPeriod = timepicker_period.Am

pub const default_view: TimepickerView = timepicker_view.Hour

// --- Configuration ---

/// Config is a public record for configuring this component.
///
pub type Config {
  Config(
    format: TimepickerFormat,
    hour: Option(Float),
    max_time: Option(TimeParts),
    min_time: Option(TimeParts),
    minute: Option(Float),
    second: Option(Float),
    show_seconds: ShowSeconds,
    period: TimepickerPeriod,
    view: TimepickerView,
  )
}

/// default_config is the default configuration for this component.
///
pub fn default_config() -> Config {
  Config(
    format: timepicker_format.OneTwo,
    hour: None,
    max_time: None,
    min_time: None,
    minute: None,
    second: None,
    show_seconds: IsNotShowSeconds,
    period: timepicker_period.Am,
    view: timepicker_view.Hour,
  )
}

// --- Constructors ---

/// from_config creates a new TimepickerDial from the given configuration.
///
pub fn from_config(config: Config) -> TimepickerDial {
  TimepickerDial(
    format: config.format,
    hour: config.hour,
    max_time: config.max_time,
    min_time: config.min_time,
    minute: config.minute,
    second: config.second,
    show_seconds: config.show_seconds,
    period: config.period,
    view: config.view,
  )
}

/// new creates a new TimepickerDial with the default configuration.
///
pub fn new() -> TimepickerDial {
  from_config(default_config())
}

// --- Setters ---

/// format sets the value of format for this TimepickerDial.
///
pub fn format(
  record: TimepickerDial,
  format: TimepickerFormat,
) -> TimepickerDial {
  TimepickerDial(..record, format: format)
}

/// hour sets the value of hour for this TimepickerDial.
///
pub fn hour(record: TimepickerDial, hour: Option(Float)) -> TimepickerDial {
  TimepickerDial(..record, hour: hour)
}

/// max_time sets the value of max_time for this TimepickerDial.
///
pub fn max_time(
  record: TimepickerDial,
  max_time: Option(TimeParts),
) -> TimepickerDial {
  TimepickerDial(..record, max_time: max_time)
}

/// min_time sets the value of min_time for this TimepickerDial.
///
pub fn min_time(
  record: TimepickerDial,
  min_time: Option(TimeParts),
) -> TimepickerDial {
  TimepickerDial(..record, min_time: min_time)
}

/// minute sets the value of minute for this TimepickerDial.
///
pub fn minute(record: TimepickerDial, minute: Option(Float)) -> TimepickerDial {
  TimepickerDial(..record, minute: minute)
}

/// second sets the value of second for this TimepickerDial.
///
pub fn second(record: TimepickerDial, second: Option(Float)) -> TimepickerDial {
  TimepickerDial(..record, second: second)
}

/// show_seconds sets the value of show_seconds for this TimepickerDial.
///
pub fn show_seconds(
  record: TimepickerDial,
  show_seconds: ShowSeconds,
) -> TimepickerDial {
  TimepickerDial(..record, show_seconds: show_seconds)
}

/// period sets the value of period for this TimepickerDial.
///
pub fn period(
  record: TimepickerDial,
  period: TimepickerPeriod,
) -> TimepickerDial {
  TimepickerDial(..record, period: period)
}

/// view sets the value of view for this TimepickerDial.
///
pub fn view(record: TimepickerDial, view: TimepickerView) -> TimepickerDial {
  TimepickerDial(..record, view: view)
}

// --- Renderers ---

/// render creates a Lustre Element for a TimepickerDial
///
pub fn render(
  model: TimepickerDial,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  element.element(
    "m3e-timepicker-dial",
    list.flatten([
      [
        attr.with_default(
          "format",
          timepicker_format.to_string(model.format),
          timepicker_format.to_string(default_format),
        ),
        attr.option(model.hour, fn(_) { "hour" }, float.to_string, default_hour),
        attr.option(
          model.max_time,
          fn(_) { "max-time" },
          time_parts.to_string,
          default_max_time,
        ),
        attr.option(
          model.min_time,
          fn(_) { "min-time" },
          time_parts.to_string,
          default_min_time,
        ),
        attr.option(
          model.minute,
          fn(_) { "minute" },
          float.to_string,
          default_minute,
        ),
        attr.option(
          model.second,
          fn(_) { "second" },
          float.to_string,
          default_second,
        ),
        attr.boolean("show-seconds", model.show_seconds == IsShowSeconds),
        attr.with_default(
          "period",
          timepicker_period.to_string(model.period),
          timepicker_period.to_string(default_period),
        ),
        attr.with_default(
          "view",
          timepicker_view.to_string(model.view),
          timepicker_view.to_string(default_view),
        ),
      ],
      attributes,
    ])
      |> list.filter(fn(a) { a != attribute.none() }),
    [],
  )
}

/// render_config creates a Lustre Element from a TimepickerDial Config
///
pub fn render_config(
  c: Config,
  attributes: List(Attribute(msg)),
) -> Element(msg) {
  render(from_config(c), attributes)
}
