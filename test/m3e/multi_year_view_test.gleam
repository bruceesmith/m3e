//// MultiYearView unit tests
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/list
import gleam/option.{None, Some}
import gleeunit/should
import lustre/attribute
import lustre/element
import m3e/date
import m3e/multi_year_view.{Config}

pub fn multi_year_view_default_config_test() {
  let cases = [
    Config(
      active: multi_year_view.IsNotActive,
      today: date.default,
      date: None,
      active_date: date.default,
      min_date: None,
      max_date: None,
    ),
  ]

  list.each(cases, fn(c) {
    let expected = c

    multi_year_view.default_config()
    |> should.equal(expected)
  })
}

pub fn multi_year_view_from_config_test() {
  let cases = [
    #(
      multi_year_view.Config(
        active: multi_year_view.IsActive,
        today: date.today_utc(),
        date: Some(date.today_utc()),
        active_date: date.today_utc(),
        min_date: Some(date.today_utc()),
        max_date: Some(date.today_utc()),
      ),
      multi_year_view.new()
        |> multi_year_view.active(multi_year_view.IsActive)
        |> multi_year_view.today(date.today_utc())
        |> multi_year_view.date(Some(date.today_utc()))
        |> multi_year_view.active_date(date.today_utc())
        |> multi_year_view.min_date(Some(date.today_utc()))
        |> multi_year_view.max_date(Some(date.today_utc())),
    ),
  ]

  list.each(cases, fn(c) {
    let #(config, expected) = c

    multi_year_view.from_config(config)
    |> should.equal(expected)
  })
}

pub fn multi_year_view_new_test() {
  let cases = [
    multi_year_view.from_config(multi_year_view.Config(
      active: multi_year_view.IsNotActive,
      today: date.default,
      date: None,
      active_date: date.default,
      min_date: None,
      max_date: None,
    )),
  ]

  list.each(cases, fn(c) {
    let expected = c

    multi_year_view.new()
    |> should.equal(expected)
  })
}

pub fn multi_year_view_active_test() {
  let mod = multi_year_view.new()
  let cases = [
    #(
      multi_year_view.IsActive,
      multi_year_view.from_config(
        multi_year_view.Config(
          ..multi_year_view.default_config(),
          active: multi_year_view.IsActive,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    multi_year_view.active(mod, field)
    |> should.equal(expected)
  })
}

pub fn multi_year_view_today_test() {
  let mod = multi_year_view.new()
  let cases = [
    #(
      date.today_utc(),
      multi_year_view.from_config(
        multi_year_view.Config(
          ..multi_year_view.default_config(),
          today: date.today_utc(),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    multi_year_view.today(mod, field)
    |> should.equal(expected)
  })
}

pub fn multi_year_view_date_test() {
  let mod = multi_year_view.new()
  let cases = [
    #(
      Some(date.today_utc()),
      multi_year_view.from_config(
        multi_year_view.Config(
          ..multi_year_view.default_config(),
          date: Some(date.today_utc()),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    multi_year_view.date(mod, field)
    |> should.equal(expected)
  })
}

pub fn multi_year_view_active_date_test() {
  let mod = multi_year_view.new()
  let cases = [
    #(
      date.today_utc(),
      multi_year_view.from_config(
        multi_year_view.Config(
          ..multi_year_view.default_config(),
          active_date: date.today_utc(),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    multi_year_view.active_date(mod, field)
    |> should.equal(expected)
  })
}

pub fn multi_year_view_min_date_test() {
  let mod = multi_year_view.new()
  let cases = [
    #(
      Some(date.today_utc()),
      multi_year_view.from_config(
        multi_year_view.Config(
          ..multi_year_view.default_config(),
          min_date: Some(date.today_utc()),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    multi_year_view.min_date(mod, field)
    |> should.equal(expected)
  })
}

pub fn multi_year_view_max_date_test() {
  let mod = multi_year_view.new()
  let cases = [
    #(
      Some(date.today_utc()),
      multi_year_view.from_config(
        multi_year_view.Config(
          ..multi_year_view.default_config(),
          max_date: Some(date.today_utc()),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    multi_year_view.max_date(mod, field)
    |> should.equal(expected)
  })
}

pub fn multi_year_view_render_test() {
  let mod = multi_year_view.new()

  let mod_active =
    multi_year_view.new() |> multi_year_view.active(multi_year_view.IsActive)
  let mod_today =
    multi_year_view.new() |> multi_year_view.today(date.today_utc())
  let mod_date =
    multi_year_view.new() |> multi_year_view.date(Some(date.today_utc()))
  let mod_active_date =
    multi_year_view.new() |> multi_year_view.active_date(date.today_utc())
  let mod_min_date =
    multi_year_view.new() |> multi_year_view.min_date(Some(date.today_utc()))
  let mod_max_date =
    multi_year_view.new() |> multi_year_view.max_date(Some(date.today_utc()))
  let cases = [
    #(#(mod, []), element.element("m3e-multi-year-view", [], [])),
    #(
      #(mod, [attribute.id("id")]),
      element.element("m3e-multi-year-view", [attribute.id("id")], []),
    ),

    #(
      #(mod_active, []),
      element.element(
        "m3e-multi-year-view",
        [attribute.attribute("active", "")],
        [],
      ),
    ),
    #(
      #(mod_today, []),
      element.element(
        "m3e-multi-year-view",
        [attribute.attribute("today", date.to_string(date.today_utc()))],
        [],
      ),
    ),
    #(
      #(mod_date, []),
      element.element(
        "m3e-multi-year-view",
        [attribute.attribute("date", date.to_string(date.today_utc()))],
        [],
      ),
    ),
    #(
      #(mod_active_date, []),
      element.element(
        "m3e-multi-year-view",
        [attribute.attribute("active-date", date.to_string(date.today_utc()))],
        [],
      ),
    ),
    #(
      #(mod_min_date, []),
      element.element(
        "m3e-multi-year-view",
        [attribute.attribute("min-date", date.to_string(date.today_utc()))],
        [],
      ),
    ),
    #(
      #(mod_max_date, []),
      element.element(
        "m3e-multi-year-view",
        [attribute.attribute("max-date", date.to_string(date.today_utc()))],
        [],
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(#(mod, attributes), expected) = c

    multi_year_view.render(mod, attributes)
    |> should.equal(expected)
  })
}
