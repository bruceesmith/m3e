//// LongPressGesture unit tests
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/list
import gleam/option.{None, Some}
import gleeunit/should
import lustre/attribute
import lustre/element
import m3e/gesture_input_button
import m3e/long_press_gesture.{Config}
import m3e/pointer_type

pub fn long_press_gesture_default_config_test() {
  let cases = [
    Config(
      for: None,
      buttons: [gesture_input_button.Primary],
      pointer_types: [pointer_type.Mouse, pointer_type.Pen, pointer_type.Touch],
      disabled: long_press_gesture.IsNotDisabled,
      priority: "1",
      pointers: 1.0,
      max_displacement: 4.0,
      min_duration: 500.0,
      max_press_interval: 120.0,
    ),
  ]

  list.each(cases, fn(c) {
    let expected = c

    long_press_gesture.default_config()
    |> should.equal(expected)
  })
}

pub fn long_press_gesture_from_config_test() {
  let cases = [
    #(
      long_press_gesture.Config(
        for: Some("test"),
        buttons: [gesture_input_button.Secondary],
        pointer_types: [pointer_type.Mouse],
        disabled: long_press_gesture.IsDisabled,
        priority: "test",
        pointers: 42.0,
        max_displacement: 42.0,
        min_duration: 42.0,
        max_press_interval: 42.0,
      ),
      long_press_gesture.new()
        |> long_press_gesture.for(Some("test"))
        |> long_press_gesture.buttons([gesture_input_button.Secondary])
        |> long_press_gesture.pointer_types([pointer_type.Mouse])
        |> long_press_gesture.disabled(long_press_gesture.IsDisabled)
        |> long_press_gesture.priority("test")
        |> long_press_gesture.pointers(42.0)
        |> long_press_gesture.max_displacement(42.0)
        |> long_press_gesture.min_duration(42.0)
        |> long_press_gesture.max_press_interval(42.0),
    ),
  ]

  list.each(cases, fn(c) {
    let #(config, expected) = c

    long_press_gesture.from_config(config)
    |> should.equal(expected)
  })
}

pub fn long_press_gesture_new_test() {
  let cases = [
    long_press_gesture.from_config(long_press_gesture.Config(
      for: None,
      buttons: [gesture_input_button.Primary],
      pointer_types: [pointer_type.Mouse, pointer_type.Pen, pointer_type.Touch],
      disabled: long_press_gesture.IsNotDisabled,
      priority: "1",
      pointers: 1.0,
      max_displacement: 4.0,
      min_duration: 500.0,
      max_press_interval: 120.0,
    )),
  ]

  list.each(cases, fn(c) {
    let expected = c

    long_press_gesture.new()
    |> should.equal(expected)
  })
}

pub fn long_press_gesture_for_test() {
  let mod = long_press_gesture.new()
  let cases = [
    #(
      Some("test"),
      long_press_gesture.from_config(
        long_press_gesture.Config(
          ..long_press_gesture.default_config(),
          for: Some("test"),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    long_press_gesture.for(mod, field)
    |> should.equal(expected)
  })
}

pub fn long_press_gesture_buttons_test() {
  let mod = long_press_gesture.new()
  let cases = [
    #(
      [gesture_input_button.Secondary],
      long_press_gesture.from_config(
        long_press_gesture.Config(
          ..long_press_gesture.default_config(),
          buttons: [gesture_input_button.Secondary],
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    long_press_gesture.buttons(mod, field)
    |> should.equal(expected)
  })
}

pub fn long_press_gesture_pointer_types_test() {
  let mod = long_press_gesture.new()
  let cases = [
    #(
      [pointer_type.Mouse],
      long_press_gesture.from_config(
        long_press_gesture.Config(
          ..long_press_gesture.default_config(),
          pointer_types: [pointer_type.Mouse],
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    long_press_gesture.pointer_types(mod, field)
    |> should.equal(expected)
  })
}

pub fn long_press_gesture_disabled_test() {
  let mod = long_press_gesture.new()
  let cases = [
    #(
      long_press_gesture.IsDisabled,
      long_press_gesture.from_config(
        long_press_gesture.Config(
          ..long_press_gesture.default_config(),
          disabled: long_press_gesture.IsDisabled,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    long_press_gesture.disabled(mod, field)
    |> should.equal(expected)
  })
}

pub fn long_press_gesture_priority_test() {
  let mod = long_press_gesture.new()
  let cases = [
    #(
      "test",
      long_press_gesture.from_config(
        long_press_gesture.Config(
          ..long_press_gesture.default_config(),
          priority: "test",
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    long_press_gesture.priority(mod, field)
    |> should.equal(expected)
  })
}

pub fn long_press_gesture_pointers_test() {
  let mod = long_press_gesture.new()
  let cases = [
    #(
      42.0,
      long_press_gesture.from_config(
        long_press_gesture.Config(
          ..long_press_gesture.default_config(),
          pointers: 42.0,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    long_press_gesture.pointers(mod, field)
    |> should.equal(expected)
  })
}

pub fn long_press_gesture_max_displacement_test() {
  let mod = long_press_gesture.new()
  let cases = [
    #(
      42.0,
      long_press_gesture.from_config(
        long_press_gesture.Config(
          ..long_press_gesture.default_config(),
          max_displacement: 42.0,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    long_press_gesture.max_displacement(mod, field)
    |> should.equal(expected)
  })
}

pub fn long_press_gesture_min_duration_test() {
  let mod = long_press_gesture.new()
  let cases = [
    #(
      42.0,
      long_press_gesture.from_config(
        long_press_gesture.Config(
          ..long_press_gesture.default_config(),
          min_duration: 42.0,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    long_press_gesture.min_duration(mod, field)
    |> should.equal(expected)
  })
}

pub fn long_press_gesture_max_press_interval_test() {
  let mod = long_press_gesture.new()
  let cases = [
    #(
      42.0,
      long_press_gesture.from_config(
        long_press_gesture.Config(
          ..long_press_gesture.default_config(),
          max_press_interval: 42.0,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    long_press_gesture.max_press_interval(mod, field)
    |> should.equal(expected)
  })
}

pub fn long_press_gesture_render_test() {
  let mod = long_press_gesture.new()

  let mod_for = long_press_gesture.new() |> long_press_gesture.for(Some("test"))
  let mod_buttons =
    long_press_gesture.new()
    |> long_press_gesture.buttons([gesture_input_button.Secondary])
  let mod_pointer_types =
    long_press_gesture.new()
    |> long_press_gesture.pointer_types([pointer_type.Mouse])
  let mod_disabled =
    long_press_gesture.new()
    |> long_press_gesture.disabled(long_press_gesture.IsDisabled)
  let mod_priority =
    long_press_gesture.new() |> long_press_gesture.priority("test")
  let mod_pointers =
    long_press_gesture.new() |> long_press_gesture.pointers(42.0)
  let mod_max_displacement =
    long_press_gesture.new() |> long_press_gesture.max_displacement(42.0)
  let mod_min_duration =
    long_press_gesture.new() |> long_press_gesture.min_duration(42.0)
  let mod_max_press_interval =
    long_press_gesture.new() |> long_press_gesture.max_press_interval(42.0)
  let cases = [
    #(#(mod, []), element.element("m3e-long-press-gesture", [], [])),
    #(
      #(mod, [attribute.id("id")]),
      element.element("m3e-long-press-gesture", [attribute.id("id")], []),
    ),

    #(
      #(mod_for, []),
      element.element(
        "m3e-long-press-gesture",
        [attribute.attribute("for", "test")],
        [],
      ),
    ),
    #(
      #(mod_buttons, []),
      element.element(
        "m3e-long-press-gesture",
        [
          attribute.attribute(
            "buttons",
            gesture_input_button.to_string(gesture_input_button.Secondary),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_pointer_types, []),
      element.element(
        "m3e-long-press-gesture",
        [
          attribute.attribute(
            "pointer-types",
            pointer_type.to_string(pointer_type.Mouse),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_disabled, []),
      element.element(
        "m3e-long-press-gesture",
        [attribute.attribute("disabled", "")],
        [],
      ),
    ),
    #(
      #(mod_priority, []),
      element.element(
        "m3e-long-press-gesture",
        [attribute.attribute("priority", "test")],
        [],
      ),
    ),
    #(
      #(mod_pointers, []),
      element.element(
        "m3e-long-press-gesture",
        [attribute.attribute("pointers", "42.0")],
        [],
      ),
    ),
    #(
      #(mod_max_displacement, []),
      element.element(
        "m3e-long-press-gesture",
        [attribute.attribute("max-displacement", "42.0")],
        [],
      ),
    ),
    #(
      #(mod_min_duration, []),
      element.element(
        "m3e-long-press-gesture",
        [attribute.attribute("min-duration", "42.0")],
        [],
      ),
    ),
    #(
      #(mod_max_press_interval, []),
      element.element(
        "m3e-long-press-gesture",
        [attribute.attribute("max-press-interval", "42.0")],
        [],
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(#(mod, attributes), expected) = c

    long_press_gesture.render(mod, attributes)
    |> should.equal(expected)
  })
}
