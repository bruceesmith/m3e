//// DateInput unit tests
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/list
import gleam/option.{None, Some}
import gleeunit/should
import lustre/attribute
import lustre/element
import m3e/date
import m3e/date_input.{Config}
import m3e/date_input_time_format
import m3e/date_input_type
import m3e/time_parts

pub fn date_input_default_config_test() {
  let cases = [
    Config(
      value: None,
      type_: date_input_type.Date,
      show_seconds: date_input.IsNotShowSeconds,
      time_format: date_input_time_format.OneTwo,
      min_date: None,
      max_date: None,
      min_time: None,
      max_time: None,
      month_label: "Month",
      day_label: "Day",
      year_label: "Year",
      hour_label: "Hour",
      minute_label: "Minute",
      second_label: "Second",
      period_label: "Period",
      disabled: date_input.IsNotDisabled,
      readonly: date_input.IsNotReadonly,
      required: date_input.IsNotRequired,
      name: "",
    ),
  ]

  list.each(cases, fn(c) {
    let expected = c

    date_input.default_config()
    |> should.equal(expected)
  })
}

pub fn date_input_from_config_test() {
  let cases = [
    #(
      date_input.Config(
        value: Some(date.today_utc()),
        type_: date_input_type.Datetime,
        show_seconds: date_input.IsShowSeconds,
        time_format: date_input_time_format.TwoFour,
        min_date: Some(date.today_utc()),
        max_date: Some(date.today_utc()),
        min_time: Some(time_parts.zero()),
        max_time: Some(time_parts.zero()),
        month_label: "test",
        day_label: "test",
        year_label: "test",
        hour_label: "test",
        minute_label: "test",
        second_label: "test",
        period_label: "test",
        disabled: date_input.IsDisabled,
        readonly: date_input.IsReadonly,
        required: date_input.IsRequired,
        name: "test",
      ),
      date_input.new()
        |> date_input.value(Some(date.today_utc()))
        |> date_input.type_(date_input_type.Datetime)
        |> date_input.show_seconds(date_input.IsShowSeconds)
        |> date_input.time_format(date_input_time_format.TwoFour)
        |> date_input.min_date(Some(date.today_utc()))
        |> date_input.max_date(Some(date.today_utc()))
        |> date_input.min_time(Some(time_parts.zero()))
        |> date_input.max_time(Some(time_parts.zero()))
        |> date_input.month_label("test")
        |> date_input.day_label("test")
        |> date_input.year_label("test")
        |> date_input.hour_label("test")
        |> date_input.minute_label("test")
        |> date_input.second_label("test")
        |> date_input.period_label("test")
        |> date_input.disabled(date_input.IsDisabled)
        |> date_input.readonly(date_input.IsReadonly)
        |> date_input.required(date_input.IsRequired)
        |> date_input.name("test"),
    ),
  ]

  list.each(cases, fn(c) {
    let #(config, expected) = c

    date_input.from_config(config)
    |> should.equal(expected)
  })
}

pub fn date_input_new_test() {
  let cases = [
    date_input.from_config(date_input.Config(
      value: None,
      type_: date_input_type.Date,
      show_seconds: date_input.IsNotShowSeconds,
      time_format: date_input_time_format.OneTwo,
      min_date: None,
      max_date: None,
      min_time: None,
      max_time: None,
      month_label: "Month",
      day_label: "Day",
      year_label: "Year",
      hour_label: "Hour",
      minute_label: "Minute",
      second_label: "Second",
      period_label: "Period",
      disabled: date_input.IsNotDisabled,
      readonly: date_input.IsNotReadonly,
      required: date_input.IsNotRequired,
      name: "",
    )),
  ]

  list.each(cases, fn(c) {
    let expected = c

    date_input.new()
    |> should.equal(expected)
  })
}

pub fn date_input_value_test() {
  let mod = date_input.new()
  let cases = [
    #(
      Some(date.today_utc()),
      date_input.from_config(
        date_input.Config(
          ..date_input.default_config(),
          value: Some(date.today_utc()),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.value(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_type__test() {
  let mod = date_input.new()
  let cases = [
    #(
      date_input_type.Datetime,
      date_input.from_config(
        date_input.Config(
          ..date_input.default_config(),
          type_: date_input_type.Datetime,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.type_(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_show_seconds_test() {
  let mod = date_input.new()
  let cases = [
    #(
      date_input.IsShowSeconds,
      date_input.from_config(
        date_input.Config(
          ..date_input.default_config(),
          show_seconds: date_input.IsShowSeconds,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.show_seconds(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_time_format_test() {
  let mod = date_input.new()
  let cases = [
    #(
      date_input_time_format.TwoFour,
      date_input.from_config(
        date_input.Config(
          ..date_input.default_config(),
          time_format: date_input_time_format.TwoFour,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.time_format(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_min_date_test() {
  let mod = date_input.new()
  let cases = [
    #(
      Some(date.today_utc()),
      date_input.from_config(
        date_input.Config(
          ..date_input.default_config(),
          min_date: Some(date.today_utc()),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.min_date(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_max_date_test() {
  let mod = date_input.new()
  let cases = [
    #(
      Some(date.today_utc()),
      date_input.from_config(
        date_input.Config(
          ..date_input.default_config(),
          max_date: Some(date.today_utc()),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.max_date(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_min_time_test() {
  let mod = date_input.new()
  let cases = [
    #(
      Some(time_parts.zero()),
      date_input.from_config(
        date_input.Config(
          ..date_input.default_config(),
          min_time: Some(time_parts.zero()),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.min_time(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_max_time_test() {
  let mod = date_input.new()
  let cases = [
    #(
      Some(time_parts.zero()),
      date_input.from_config(
        date_input.Config(
          ..date_input.default_config(),
          max_time: Some(time_parts.zero()),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.max_time(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_month_label_test() {
  let mod = date_input.new()
  let cases = [
    #(
      "test",
      date_input.from_config(
        date_input.Config(..date_input.default_config(), month_label: "test"),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.month_label(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_day_label_test() {
  let mod = date_input.new()
  let cases = [
    #(
      "test",
      date_input.from_config(
        date_input.Config(..date_input.default_config(), day_label: "test"),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.day_label(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_year_label_test() {
  let mod = date_input.new()
  let cases = [
    #(
      "test",
      date_input.from_config(
        date_input.Config(..date_input.default_config(), year_label: "test"),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.year_label(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_hour_label_test() {
  let mod = date_input.new()
  let cases = [
    #(
      "test",
      date_input.from_config(
        date_input.Config(..date_input.default_config(), hour_label: "test"),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.hour_label(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_minute_label_test() {
  let mod = date_input.new()
  let cases = [
    #(
      "test",
      date_input.from_config(
        date_input.Config(..date_input.default_config(), minute_label: "test"),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.minute_label(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_second_label_test() {
  let mod = date_input.new()
  let cases = [
    #(
      "test",
      date_input.from_config(
        date_input.Config(..date_input.default_config(), second_label: "test"),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.second_label(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_period_label_test() {
  let mod = date_input.new()
  let cases = [
    #(
      "test",
      date_input.from_config(
        date_input.Config(..date_input.default_config(), period_label: "test"),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.period_label(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_disabled_test() {
  let mod = date_input.new()
  let cases = [
    #(
      date_input.IsDisabled,
      date_input.from_config(
        date_input.Config(
          ..date_input.default_config(),
          disabled: date_input.IsDisabled,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.disabled(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_readonly_test() {
  let mod = date_input.new()
  let cases = [
    #(
      date_input.IsReadonly,
      date_input.from_config(
        date_input.Config(
          ..date_input.default_config(),
          readonly: date_input.IsReadonly,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.readonly(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_required_test() {
  let mod = date_input.new()
  let cases = [
    #(
      date_input.IsRequired,
      date_input.from_config(
        date_input.Config(
          ..date_input.default_config(),
          required: date_input.IsRequired,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.required(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_name_test() {
  let mod = date_input.new()
  let cases = [
    #(
      "test",
      date_input.from_config(
        date_input.Config(..date_input.default_config(), name: "test"),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    date_input.name(mod, field)
    |> should.equal(expected)
  })
}

pub fn date_input_render_test() {
  let mod = date_input.new()

  let mod_value = date_input.new() |> date_input.value(Some(date.today_utc()))
  let mod_type_ = date_input.new() |> date_input.type_(date_input_type.Datetime)
  let mod_show_seconds =
    date_input.new() |> date_input.show_seconds(date_input.IsShowSeconds)
  let mod_time_format =
    date_input.new() |> date_input.time_format(date_input_time_format.TwoFour)
  let mod_min_date =
    date_input.new() |> date_input.min_date(Some(date.today_utc()))
  let mod_max_date =
    date_input.new() |> date_input.max_date(Some(date.today_utc()))
  let mod_min_time =
    date_input.new() |> date_input.min_time(Some(time_parts.zero()))
  let mod_max_time =
    date_input.new() |> date_input.max_time(Some(time_parts.zero()))
  let mod_month_label = date_input.new() |> date_input.month_label("test")
  let mod_day_label = date_input.new() |> date_input.day_label("test")
  let mod_year_label = date_input.new() |> date_input.year_label("test")
  let mod_hour_label = date_input.new() |> date_input.hour_label("test")
  let mod_minute_label = date_input.new() |> date_input.minute_label("test")
  let mod_second_label = date_input.new() |> date_input.second_label("test")
  let mod_period_label = date_input.new() |> date_input.period_label("test")
  let mod_disabled =
    date_input.new() |> date_input.disabled(date_input.IsDisabled)
  let mod_readonly =
    date_input.new() |> date_input.readonly(date_input.IsReadonly)
  let mod_required =
    date_input.new() |> date_input.required(date_input.IsRequired)
  let mod_name = date_input.new() |> date_input.name("test")
  let cases = [
    #(#(mod, []), element.element("m3e-date-input", [], [])),
    #(
      #(mod, [attribute.id("id")]),
      element.element("m3e-date-input", [attribute.id("id")], []),
    ),

    #(
      #(mod_value, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("value", date.to_string(date.today_utc()))],
        [],
      ),
    ),
    #(
      #(mod_type_, []),
      element.element(
        "m3e-date-input",
        [
          attribute.attribute(
            "type",
            date_input_type.to_string(date_input_type.Datetime),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_show_seconds, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("show-seconds", "")],
        [],
      ),
    ),
    #(
      #(mod_time_format, []),
      element.element(
        "m3e-date-input",
        [
          attribute.attribute(
            "time-format",
            date_input_time_format.to_string(date_input_time_format.TwoFour),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_min_date, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("min-date", date.to_string(date.today_utc()))],
        [],
      ),
    ),
    #(
      #(mod_max_date, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("max-date", date.to_string(date.today_utc()))],
        [],
      ),
    ),
    #(
      #(mod_min_time, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("min-time", time_parts.zero_string())],
        [],
      ),
    ),
    #(
      #(mod_max_time, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("max-time", time_parts.zero_string())],
        [],
      ),
    ),
    #(
      #(mod_month_label, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("month-label", "test")],
        [],
      ),
    ),
    #(
      #(mod_day_label, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("day-label", "test")],
        [],
      ),
    ),
    #(
      #(mod_year_label, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("year-label", "test")],
        [],
      ),
    ),
    #(
      #(mod_hour_label, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("hour-label", "test")],
        [],
      ),
    ),
    #(
      #(mod_minute_label, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("minute-label", "test")],
        [],
      ),
    ),
    #(
      #(mod_second_label, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("second-label", "test")],
        [],
      ),
    ),
    #(
      #(mod_period_label, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("period-label", "test")],
        [],
      ),
    ),
    #(
      #(mod_disabled, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("disabled", "")],
        [],
      ),
    ),
    #(
      #(mod_readonly, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("readonly", "")],
        [],
      ),
    ),
    #(
      #(mod_required, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("required", "")],
        [],
      ),
    ),
    #(
      #(mod_name, []),
      element.element(
        "m3e-date-input",
        [attribute.attribute("name", "test")],
        [],
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(#(mod, attributes), expected) = c

    date_input.render(mod, attributes)
    |> should.equal(expected)
  })
}
