//// TimepickerInput unit tests
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/list
import gleam/option.{None, Some}
import gleeunit/should
import lustre/attribute
import lustre/element
import m3e/time_parts
import m3e/timepicker_format
import m3e/timepicker_input.{Config}
import m3e/timepicker_orientation
import m3e/timepicker_period
import m3e/timepicker_view

pub fn timepicker_input_default_config_test() {
  let cases = [
    Config(
      format: timepicker_format.OneTwo,
      hide_labels: timepicker_input.IsNotHideLabels,
      hour: None,
      max_time: None,
      min_time: None,
      minute: None,
      second: None,
      show_seconds: timepicker_input.IsNotShowSeconds,
      orientation: timepicker_orientation.Horizontal,
      period: timepicker_period.Am,
      view: timepicker_view.Hour,
      hour_label: "Hour",
      minute_label: "Minute",
      second_label: "Second",
      period_toggle_label: "AM or PM",
      for: None,
    ),
  ]

  list.each(cases, fn(c) {
    let expected = c

    timepicker_input.default_config()
    |> should.equal(expected)
  })
}

pub fn timepicker_input_from_config_test() {
  let cases = [
    #(
      timepicker_input.Config(
        format: timepicker_format.TwoFour,
        hide_labels: timepicker_input.IsHideLabels,
        hour: Some(42.0),
        max_time: Some(time_parts.zero()),
        min_time: Some(time_parts.zero()),
        minute: Some(42.0),
        second: Some(42.0),
        show_seconds: timepicker_input.IsShowSeconds,
        orientation: timepicker_orientation.Vertical,
        period: timepicker_period.Pm,
        view: timepicker_view.Minute,
        hour_label: "test",
        minute_label: "test",
        second_label: "test",
        period_toggle_label: "test",
        for: Some("test"),
      ),
      timepicker_input.new()
        |> timepicker_input.format(timepicker_format.TwoFour)
        |> timepicker_input.hide_labels(timepicker_input.IsHideLabels)
        |> timepicker_input.hour(Some(42.0))
        |> timepicker_input.max_time(Some(time_parts.zero()))
        |> timepicker_input.min_time(Some(time_parts.zero()))
        |> timepicker_input.minute(Some(42.0))
        |> timepicker_input.second(Some(42.0))
        |> timepicker_input.show_seconds(timepicker_input.IsShowSeconds)
        |> timepicker_input.orientation(timepicker_orientation.Vertical)
        |> timepicker_input.period(timepicker_period.Pm)
        |> timepicker_input.view(timepicker_view.Minute)
        |> timepicker_input.hour_label("test")
        |> timepicker_input.minute_label("test")
        |> timepicker_input.second_label("test")
        |> timepicker_input.period_toggle_label("test")
        |> timepicker_input.for(Some("test")),
    ),
  ]

  list.each(cases, fn(c) {
    let #(config, expected) = c

    timepicker_input.from_config(config)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_new_test() {
  let cases = [
    timepicker_input.from_config(timepicker_input.Config(
      format: timepicker_format.OneTwo,
      hide_labels: timepicker_input.IsNotHideLabels,
      hour: None,
      max_time: None,
      min_time: None,
      minute: None,
      second: None,
      show_seconds: timepicker_input.IsNotShowSeconds,
      orientation: timepicker_orientation.Horizontal,
      period: timepicker_period.Am,
      view: timepicker_view.Hour,
      hour_label: "Hour",
      minute_label: "Minute",
      second_label: "Second",
      period_toggle_label: "AM or PM",
      for: None,
    )),
  ]

  list.each(cases, fn(c) {
    let expected = c

    timepicker_input.new()
    |> should.equal(expected)
  })
}

pub fn timepicker_input_format_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      timepicker_format.TwoFour,
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          format: timepicker_format.TwoFour,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.format(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_hide_labels_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      timepicker_input.IsHideLabels,
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          hide_labels: timepicker_input.IsHideLabels,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.hide_labels(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_hour_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      Some(42.0),
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          hour: Some(42.0),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.hour(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_max_time_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      Some(time_parts.zero()),
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          max_time: Some(time_parts.zero()),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.max_time(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_min_time_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      Some(time_parts.zero()),
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          min_time: Some(time_parts.zero()),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.min_time(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_minute_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      Some(42.0),
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          minute: Some(42.0),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.minute(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_second_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      Some(42.0),
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          second: Some(42.0),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.second(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_show_seconds_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      timepicker_input.IsShowSeconds,
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          show_seconds: timepicker_input.IsShowSeconds,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.show_seconds(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_orientation_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      timepicker_orientation.Vertical,
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          orientation: timepicker_orientation.Vertical,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.orientation(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_period_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      timepicker_period.Pm,
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          period: timepicker_period.Pm,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.period(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_view_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      timepicker_view.Minute,
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          view: timepicker_view.Minute,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.view(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_hour_label_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      "test",
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          hour_label: "test",
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.hour_label(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_minute_label_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      "test",
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          minute_label: "test",
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.minute_label(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_second_label_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      "test",
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          second_label: "test",
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.second_label(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_period_toggle_label_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      "test",
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          period_toggle_label: "test",
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.period_toggle_label(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_for_test() {
  let mod = timepicker_input.new()
  let cases = [
    #(
      Some("test"),
      timepicker_input.from_config(
        timepicker_input.Config(
          ..timepicker_input.default_config(),
          for: Some("test"),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_input.for(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_input_render_test() {
  let mod = timepicker_input.new()

  let mod_format =
    timepicker_input.new() |> timepicker_input.format(timepicker_format.TwoFour)
  let mod_hide_labels =
    timepicker_input.new()
    |> timepicker_input.hide_labels(timepicker_input.IsHideLabels)
  let mod_hour = timepicker_input.new() |> timepicker_input.hour(Some(42.0))
  let mod_max_time =
    timepicker_input.new() |> timepicker_input.max_time(Some(time_parts.zero()))
  let mod_min_time =
    timepicker_input.new() |> timepicker_input.min_time(Some(time_parts.zero()))
  let mod_minute = timepicker_input.new() |> timepicker_input.minute(Some(42.0))
  let mod_second = timepicker_input.new() |> timepicker_input.second(Some(42.0))
  let mod_show_seconds =
    timepicker_input.new()
    |> timepicker_input.show_seconds(timepicker_input.IsShowSeconds)
  let mod_orientation =
    timepicker_input.new()
    |> timepicker_input.orientation(timepicker_orientation.Vertical)
  let mod_period =
    timepicker_input.new() |> timepicker_input.period(timepicker_period.Pm)
  let mod_view =
    timepicker_input.new() |> timepicker_input.view(timepicker_view.Minute)
  let mod_hour_label =
    timepicker_input.new() |> timepicker_input.hour_label("test")
  let mod_minute_label =
    timepicker_input.new() |> timepicker_input.minute_label("test")
  let mod_second_label =
    timepicker_input.new() |> timepicker_input.second_label("test")
  let mod_period_toggle_label =
    timepicker_input.new() |> timepicker_input.period_toggle_label("test")
  let mod_for = timepicker_input.new() |> timepicker_input.for(Some("test"))
  let cases = [
    #(#(mod, []), element.element("m3e-timepicker-input", [], [])),
    #(
      #(mod, [attribute.id("id")]),
      element.element("m3e-timepicker-input", [attribute.id("id")], []),
    ),

    #(
      #(mod_format, []),
      element.element(
        "m3e-timepicker-input",
        [
          attribute.attribute(
            "format",
            timepicker_format.to_string(timepicker_format.TwoFour),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_hide_labels, []),
      element.element(
        "m3e-timepicker-input",
        [attribute.attribute("hide-labels", "")],
        [],
      ),
    ),
    #(
      #(mod_hour, []),
      element.element(
        "m3e-timepicker-input",
        [attribute.attribute("hour", "42.0")],
        [],
      ),
    ),
    #(
      #(mod_max_time, []),
      element.element(
        "m3e-timepicker-input",
        [attribute.attribute("max-time", time_parts.zero_string())],
        [],
      ),
    ),
    #(
      #(mod_min_time, []),
      element.element(
        "m3e-timepicker-input",
        [attribute.attribute("min-time", time_parts.zero_string())],
        [],
      ),
    ),
    #(
      #(mod_minute, []),
      element.element(
        "m3e-timepicker-input",
        [attribute.attribute("minute", "42.0")],
        [],
      ),
    ),
    #(
      #(mod_second, []),
      element.element(
        "m3e-timepicker-input",
        [attribute.attribute("second", "42.0")],
        [],
      ),
    ),
    #(
      #(mod_show_seconds, []),
      element.element(
        "m3e-timepicker-input",
        [attribute.attribute("show-seconds", "")],
        [],
      ),
    ),
    #(
      #(mod_orientation, []),
      element.element(
        "m3e-timepicker-input",
        [
          attribute.attribute(
            "orientation",
            timepicker_orientation.to_string(timepicker_orientation.Vertical),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_period, []),
      element.element(
        "m3e-timepicker-input",
        [
          attribute.attribute(
            "period",
            timepicker_period.to_string(timepicker_period.Pm),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_view, []),
      element.element(
        "m3e-timepicker-input",
        [
          attribute.attribute(
            "view",
            timepicker_view.to_string(timepicker_view.Minute),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_hour_label, []),
      element.element(
        "m3e-timepicker-input",
        [attribute.attribute("hour-label", "test")],
        [],
      ),
    ),
    #(
      #(mod_minute_label, []),
      element.element(
        "m3e-timepicker-input",
        [attribute.attribute("minute-label", "test")],
        [],
      ),
    ),
    #(
      #(mod_second_label, []),
      element.element(
        "m3e-timepicker-input",
        [attribute.attribute("second-label", "test")],
        [],
      ),
    ),
    #(
      #(mod_period_toggle_label, []),
      element.element(
        "m3e-timepicker-input",
        [attribute.attribute("period-toggle-label", "test")],
        [],
      ),
    ),
    #(
      #(mod_for, []),
      element.element(
        "m3e-timepicker-input",
        [attribute.attribute("for", "test")],
        [],
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(#(mod, attributes), expected) = c

    timepicker_input.render(mod, attributes)
    |> should.equal(expected)
  })
}
