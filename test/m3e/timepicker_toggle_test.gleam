//// TimepickerToggle unit tests
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/list
import gleam/option.{None, Some}
import gleeunit/should
import lustre/attribute
import lustre/element
import m3e/timepicker_toggle

pub fn timepicker_toggle_render_test() {
  let mod = timepicker_toggle.new(None)
  let mod_for = timepicker_toggle.new(Some("test"))

  let cases = [
    #(#(mod, []), element.element("m3e-timepicker-toggle", [], [])),
    #(
      #(mod, [attribute.id("id")]),
      element.element("m3e-timepicker-toggle", [attribute.id("id")], []),
    ),

    #(
      #(mod_for, []),
      element.element(
        "m3e-timepicker-toggle",
        [attribute.attribute("for", "test")],
        [],
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(#(mod, attributes), expected) = c

    timepicker_toggle.render(mod, attributes)
    |> should.equal(expected)
  })
}
