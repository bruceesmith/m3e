//// RotateGesture unit tests
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/list
import gleam/option.{None, Some}
import gleeunit/should
import lustre/attribute
import lustre/element
import m3e/gesture_input_button
import m3e/pan_gesture_activation_mode
import m3e/pointer_type
import m3e/rotate_gesture.{Config}

pub fn rotate_gesture_default_config_test() {
  let cases = [
    Config(
      for: None,
      buttons: [gesture_input_button.Primary],
      pointer_types: [pointer_type.Mouse, pointer_type.Pen, pointer_type.Touch],
      disabled: rotate_gesture.IsNotDisabled,
      priority: "1",
      activation_mode: pan_gesture_activation_mode.Press,
      pointers: 2.0,
      min_displacement: 4.0,
      max_press_interval: 120.0,
    ),
  ]

  list.each(cases, fn(c) {
    let expected = c

    rotate_gesture.default_config()
    |> should.equal(expected)
  })
}

pub fn rotate_gesture_from_config_test() {
  let cases = [
    #(
      rotate_gesture.Config(
        for: Some("test"),
        buttons: [gesture_input_button.Secondary],
        pointer_types: [pointer_type.Mouse],
        disabled: rotate_gesture.IsDisabled,
        priority: "test",
        activation_mode: pan_gesture_activation_mode.Move,
        pointers: 42.0,
        min_displacement: 42.0,
        max_press_interval: 42.0,
      ),
      rotate_gesture.new()
        |> rotate_gesture.for(Some("test"))
        |> rotate_gesture.buttons([gesture_input_button.Secondary])
        |> rotate_gesture.pointer_types([pointer_type.Mouse])
        |> rotate_gesture.disabled(rotate_gesture.IsDisabled)
        |> rotate_gesture.priority("test")
        |> rotate_gesture.activation_mode(pan_gesture_activation_mode.Move)
        |> rotate_gesture.pointers(42.0)
        |> rotate_gesture.min_displacement(42.0)
        |> rotate_gesture.max_press_interval(42.0),
    ),
  ]

  list.each(cases, fn(c) {
    let #(config, expected) = c

    rotate_gesture.from_config(config)
    |> should.equal(expected)
  })
}

pub fn rotate_gesture_new_test() {
  let cases = [
    rotate_gesture.from_config(rotate_gesture.Config(
      for: None,
      buttons: [gesture_input_button.Primary],
      pointer_types: [pointer_type.Mouse, pointer_type.Pen, pointer_type.Touch],
      disabled: rotate_gesture.IsNotDisabled,
      priority: "1",
      activation_mode: pan_gesture_activation_mode.Press,
      pointers: 2.0,
      min_displacement: 4.0,
      max_press_interval: 120.0,
    )),
  ]

  list.each(cases, fn(c) {
    let expected = c

    rotate_gesture.new()
    |> should.equal(expected)
  })
}

pub fn rotate_gesture_for_test() {
  let mod = rotate_gesture.new()
  let cases = [
    #(
      Some("test"),
      rotate_gesture.from_config(
        rotate_gesture.Config(
          ..rotate_gesture.default_config(),
          for: Some("test"),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    rotate_gesture.for(mod, field)
    |> should.equal(expected)
  })
}

pub fn rotate_gesture_buttons_test() {
  let mod = rotate_gesture.new()
  let cases = [
    #(
      [gesture_input_button.Secondary],
      rotate_gesture.from_config(
        rotate_gesture.Config(..rotate_gesture.default_config(), buttons: [
          gesture_input_button.Secondary,
        ]),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    rotate_gesture.buttons(mod, field)
    |> should.equal(expected)
  })
}

pub fn rotate_gesture_pointer_types_test() {
  let mod = rotate_gesture.new()
  let cases = [
    #(
      [pointer_type.Mouse],
      rotate_gesture.from_config(
        rotate_gesture.Config(..rotate_gesture.default_config(), pointer_types: [
          pointer_type.Mouse,
        ]),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    rotate_gesture.pointer_types(mod, field)
    |> should.equal(expected)
  })
}

pub fn rotate_gesture_disabled_test() {
  let mod = rotate_gesture.new()
  let cases = [
    #(
      rotate_gesture.IsDisabled,
      rotate_gesture.from_config(
        rotate_gesture.Config(
          ..rotate_gesture.default_config(),
          disabled: rotate_gesture.IsDisabled,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    rotate_gesture.disabled(mod, field)
    |> should.equal(expected)
  })
}

pub fn rotate_gesture_priority_test() {
  let mod = rotate_gesture.new()
  let cases = [
    #(
      "test",
      rotate_gesture.from_config(
        rotate_gesture.Config(
          ..rotate_gesture.default_config(),
          priority: "test",
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    rotate_gesture.priority(mod, field)
    |> should.equal(expected)
  })
}

pub fn rotate_gesture_activation_mode_test() {
  let mod = rotate_gesture.new()
  let cases = [
    #(
      pan_gesture_activation_mode.Move,
      rotate_gesture.from_config(
        rotate_gesture.Config(
          ..rotate_gesture.default_config(),
          activation_mode: pan_gesture_activation_mode.Move,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    rotate_gesture.activation_mode(mod, field)
    |> should.equal(expected)
  })
}

pub fn rotate_gesture_pointers_test() {
  let mod = rotate_gesture.new()
  let cases = [
    #(
      42.0,
      rotate_gesture.from_config(
        rotate_gesture.Config(..rotate_gesture.default_config(), pointers: 42.0),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    rotate_gesture.pointers(mod, field)
    |> should.equal(expected)
  })
}

pub fn rotate_gesture_min_displacement_test() {
  let mod = rotate_gesture.new()
  let cases = [
    #(
      42.0,
      rotate_gesture.from_config(
        rotate_gesture.Config(
          ..rotate_gesture.default_config(),
          min_displacement: 42.0,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    rotate_gesture.min_displacement(mod, field)
    |> should.equal(expected)
  })
}

pub fn rotate_gesture_max_press_interval_test() {
  let mod = rotate_gesture.new()
  let cases = [
    #(
      42.0,
      rotate_gesture.from_config(
        rotate_gesture.Config(
          ..rotate_gesture.default_config(),
          max_press_interval: 42.0,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    rotate_gesture.max_press_interval(mod, field)
    |> should.equal(expected)
  })
}

pub fn rotate_gesture_render_test() {
  let mod = rotate_gesture.new()

  let mod_for = rotate_gesture.new() |> rotate_gesture.for(Some("test"))
  let mod_buttons =
    rotate_gesture.new()
    |> rotate_gesture.buttons([gesture_input_button.Secondary])
  let mod_pointer_types =
    rotate_gesture.new() |> rotate_gesture.pointer_types([pointer_type.Mouse])
  let mod_disabled =
    rotate_gesture.new() |> rotate_gesture.disabled(rotate_gesture.IsDisabled)
  let mod_priority = rotate_gesture.new() |> rotate_gesture.priority("test")
  let mod_activation_mode =
    rotate_gesture.new()
    |> rotate_gesture.activation_mode(pan_gesture_activation_mode.Move)
  let mod_pointers = rotate_gesture.new() |> rotate_gesture.pointers(42.0)
  let mod_min_displacement =
    rotate_gesture.new() |> rotate_gesture.min_displacement(42.0)
  let mod_max_press_interval =
    rotate_gesture.new() |> rotate_gesture.max_press_interval(42.0)
  let cases = [
    #(#(mod, []), element.element("m3e-rotate-gesture", [], [])),
    #(
      #(mod, [attribute.id("id")]),
      element.element("m3e-rotate-gesture", [attribute.id("id")], []),
    ),

    #(
      #(mod_for, []),
      element.element(
        "m3e-rotate-gesture",
        [attribute.attribute("for", "test")],
        [],
      ),
    ),
    #(
      #(mod_buttons, []),
      element.element(
        "m3e-rotate-gesture",
        [
          attribute.attribute(
            "buttons",
            gesture_input_button.to_string(gesture_input_button.Secondary),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_pointer_types, []),
      element.element(
        "m3e-rotate-gesture",
        [
          attribute.attribute(
            "pointer-types",
            pointer_type.to_string(pointer_type.Mouse),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_disabled, []),
      element.element(
        "m3e-rotate-gesture",
        [attribute.attribute("disabled", "")],
        [],
      ),
    ),
    #(
      #(mod_priority, []),
      element.element(
        "m3e-rotate-gesture",
        [attribute.attribute("priority", "test")],
        [],
      ),
    ),
    #(
      #(mod_activation_mode, []),
      element.element(
        "m3e-rotate-gesture",
        [
          attribute.attribute(
            "activation-mode",
            pan_gesture_activation_mode.to_string(
              pan_gesture_activation_mode.Move,
            ),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_pointers, []),
      element.element(
        "m3e-rotate-gesture",
        [attribute.attribute("pointers", "42.0")],
        [],
      ),
    ),
    #(
      #(mod_min_displacement, []),
      element.element(
        "m3e-rotate-gesture",
        [attribute.attribute("min-displacement", "42.0")],
        [],
      ),
    ),
    #(
      #(mod_max_press_interval, []),
      element.element(
        "m3e-rotate-gesture",
        [attribute.attribute("max-press-interval", "42.0")],
        [],
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(#(mod, attributes), expected) = c

    rotate_gesture.render(mod, attributes)
    |> should.equal(expected)
  })
}
