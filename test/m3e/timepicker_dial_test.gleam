//// TimepickerDial unit tests
////
//// This file was generated by m3e/generator
////
////          DO NOT EDIT
////

import gleam/list
import gleam/option.{None, Some}
import gleeunit/should
import lustre/attribute
import lustre/element
import m3e/time_parts
import m3e/timepicker_dial.{Config}
import m3e/timepicker_format
import m3e/timepicker_period
import m3e/timepicker_view

pub fn timepicker_dial_default_config_test() {
  let cases = [
    Config(
      format: timepicker_format.OneTwo,
      hour: None,
      max_time: None,
      min_time: None,
      minute: None,
      second: None,
      show_seconds: timepicker_dial.IsNotShowSeconds,
      period: timepicker_period.Am,
      view: timepicker_view.Hour,
    ),
  ]

  list.each(cases, fn(c) {
    let expected = c

    timepicker_dial.default_config()
    |> should.equal(expected)
  })
}

pub fn timepicker_dial_from_config_test() {
  let cases = [
    #(
      timepicker_dial.Config(
        format: timepicker_format.TwoFour,
        hour: Some(42.0),
        max_time: Some(time_parts.zero()),
        min_time: Some(time_parts.zero()),
        minute: Some(42.0),
        second: Some(42.0),
        show_seconds: timepicker_dial.IsShowSeconds,
        period: timepicker_period.Pm,
        view: timepicker_view.Minute,
      ),
      timepicker_dial.new()
        |> timepicker_dial.format(timepicker_format.TwoFour)
        |> timepicker_dial.hour(Some(42.0))
        |> timepicker_dial.max_time(Some(time_parts.zero()))
        |> timepicker_dial.min_time(Some(time_parts.zero()))
        |> timepicker_dial.minute(Some(42.0))
        |> timepicker_dial.second(Some(42.0))
        |> timepicker_dial.show_seconds(timepicker_dial.IsShowSeconds)
        |> timepicker_dial.period(timepicker_period.Pm)
        |> timepicker_dial.view(timepicker_view.Minute),
    ),
  ]

  list.each(cases, fn(c) {
    let #(config, expected) = c

    timepicker_dial.from_config(config)
    |> should.equal(expected)
  })
}

pub fn timepicker_dial_new_test() {
  let cases = [
    timepicker_dial.from_config(timepicker_dial.Config(
      format: timepicker_format.OneTwo,
      hour: None,
      max_time: None,
      min_time: None,
      minute: None,
      second: None,
      show_seconds: timepicker_dial.IsNotShowSeconds,
      period: timepicker_period.Am,
      view: timepicker_view.Hour,
    )),
  ]

  list.each(cases, fn(c) {
    let expected = c

    timepicker_dial.new()
    |> should.equal(expected)
  })
}

pub fn timepicker_dial_format_test() {
  let mod = timepicker_dial.new()
  let cases = [
    #(
      timepicker_format.TwoFour,
      timepicker_dial.from_config(
        timepicker_dial.Config(
          ..timepicker_dial.default_config(),
          format: timepicker_format.TwoFour,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_dial.format(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_dial_hour_test() {
  let mod = timepicker_dial.new()
  let cases = [
    #(
      Some(42.0),
      timepicker_dial.from_config(
        timepicker_dial.Config(
          ..timepicker_dial.default_config(),
          hour: Some(42.0),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_dial.hour(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_dial_max_time_test() {
  let mod = timepicker_dial.new()
  let cases = [
    #(
      Some(time_parts.zero()),
      timepicker_dial.from_config(
        timepicker_dial.Config(
          ..timepicker_dial.default_config(),
          max_time: Some(time_parts.zero()),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_dial.max_time(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_dial_min_time_test() {
  let mod = timepicker_dial.new()
  let cases = [
    #(
      Some(time_parts.zero()),
      timepicker_dial.from_config(
        timepicker_dial.Config(
          ..timepicker_dial.default_config(),
          min_time: Some(time_parts.zero()),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_dial.min_time(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_dial_minute_test() {
  let mod = timepicker_dial.new()
  let cases = [
    #(
      Some(42.0),
      timepicker_dial.from_config(
        timepicker_dial.Config(
          ..timepicker_dial.default_config(),
          minute: Some(42.0),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_dial.minute(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_dial_second_test() {
  let mod = timepicker_dial.new()
  let cases = [
    #(
      Some(42.0),
      timepicker_dial.from_config(
        timepicker_dial.Config(
          ..timepicker_dial.default_config(),
          second: Some(42.0),
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_dial.second(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_dial_show_seconds_test() {
  let mod = timepicker_dial.new()
  let cases = [
    #(
      timepicker_dial.IsShowSeconds,
      timepicker_dial.from_config(
        timepicker_dial.Config(
          ..timepicker_dial.default_config(),
          show_seconds: timepicker_dial.IsShowSeconds,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_dial.show_seconds(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_dial_period_test() {
  let mod = timepicker_dial.new()
  let cases = [
    #(
      timepicker_period.Pm,
      timepicker_dial.from_config(
        timepicker_dial.Config(
          ..timepicker_dial.default_config(),
          period: timepicker_period.Pm,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_dial.period(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_dial_view_test() {
  let mod = timepicker_dial.new()
  let cases = [
    #(
      timepicker_view.Minute,
      timepicker_dial.from_config(
        timepicker_dial.Config(
          ..timepicker_dial.default_config(),
          view: timepicker_view.Minute,
        ),
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(field, expected) = c

    timepicker_dial.view(mod, field)
    |> should.equal(expected)
  })
}

pub fn timepicker_dial_render_test() {
  let mod = timepicker_dial.new()

  let mod_format =
    timepicker_dial.new() |> timepicker_dial.format(timepicker_format.TwoFour)
  let mod_hour = timepicker_dial.new() |> timepicker_dial.hour(Some(42.0))
  let mod_max_time =
    timepicker_dial.new() |> timepicker_dial.max_time(Some(time_parts.zero()))
  let mod_min_time =
    timepicker_dial.new() |> timepicker_dial.min_time(Some(time_parts.zero()))
  let mod_minute = timepicker_dial.new() |> timepicker_dial.minute(Some(42.0))
  let mod_second = timepicker_dial.new() |> timepicker_dial.second(Some(42.0))
  let mod_show_seconds =
    timepicker_dial.new()
    |> timepicker_dial.show_seconds(timepicker_dial.IsShowSeconds)
  let mod_period =
    timepicker_dial.new() |> timepicker_dial.period(timepicker_period.Pm)
  let mod_view =
    timepicker_dial.new() |> timepicker_dial.view(timepicker_view.Minute)
  let cases = [
    #(#(mod, []), element.element("m3e-timepicker-dial", [], [])),
    #(
      #(mod, [attribute.id("id")]),
      element.element("m3e-timepicker-dial", [attribute.id("id")], []),
    ),

    #(
      #(mod_format, []),
      element.element(
        "m3e-timepicker-dial",
        [
          attribute.attribute(
            "format",
            timepicker_format.to_string(timepicker_format.TwoFour),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_hour, []),
      element.element(
        "m3e-timepicker-dial",
        [attribute.attribute("hour", "42.0")],
        [],
      ),
    ),
    #(
      #(mod_max_time, []),
      element.element(
        "m3e-timepicker-dial",
        [attribute.attribute("max-time", time_parts.zero_string())],
        [],
      ),
    ),
    #(
      #(mod_min_time, []),
      element.element(
        "m3e-timepicker-dial",
        [attribute.attribute("min-time", time_parts.zero_string())],
        [],
      ),
    ),
    #(
      #(mod_minute, []),
      element.element(
        "m3e-timepicker-dial",
        [attribute.attribute("minute", "42.0")],
        [],
      ),
    ),
    #(
      #(mod_second, []),
      element.element(
        "m3e-timepicker-dial",
        [attribute.attribute("second", "42.0")],
        [],
      ),
    ),
    #(
      #(mod_show_seconds, []),
      element.element(
        "m3e-timepicker-dial",
        [attribute.attribute("show-seconds", "")],
        [],
      ),
    ),
    #(
      #(mod_period, []),
      element.element(
        "m3e-timepicker-dial",
        [
          attribute.attribute(
            "period",
            timepicker_period.to_string(timepicker_period.Pm),
          ),
        ],
        [],
      ),
    ),
    #(
      #(mod_view, []),
      element.element(
        "m3e-timepicker-dial",
        [
          attribute.attribute(
            "view",
            timepicker_view.to_string(timepicker_view.Minute),
          ),
        ],
        [],
      ),
    ),
  ]

  list.each(cases, fn(c) {
    let #(#(mod, attributes), expected) = c

    timepicker_dial.render(mod, attributes)
    |> should.equal(expected)
  })
}
